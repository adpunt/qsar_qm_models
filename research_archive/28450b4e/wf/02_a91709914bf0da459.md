# DECISION LEDGER — distilled from the four state documents

Sources read in full: `/Users/apunt/repos/qsar_qm_models/DISCUSSION_TRACKER.md` (270 L), `/Users/apunt/repos/qsar_qm_models/immediate_next_steps.md` (1632 L), `/Users/apunt/repos/qsar_qm_models/REVISION_STATUS.md` (155 L), `/Users/apunt/repos/qsar_qm_models/DISCUSSION_REWORK.md` (219 L).

**Structural note before the ledger:** the task asks for "items 0..11 in immediate_next_steps". **No such numbered 0–11 list exists in that file as it now stands.** Its numbered registers are §D (D1, D1b, D2, D3, D4, D5), §K (K1–K10), §M (M1–M4), §O1 (1–15), §H1 (thread IDs), §G (INS-T1–T24). All are ledgered below under their real IDs.

---

## A. THE LEDGER

### A1. DISCUSSION_TRACKER D1–D11 (authority on script changes; status table at DISCUSSION_TRACKER.md:143–155)

| ID | The question, in plain English | Status | What was decided, and on what evidence |
|---|---|---|---|
| **D1** | The combined validation figure squashes all six noise types and all three datasets into one bar per model. Do we redraw it split by noise type, or relabel it honestly as an average and move the paper's claims onto a different figure? | **OPEN** (🔶 discussing) — DISCUSSION_TRACKER.md:145 | Not decided. Evidence gathered: model robustness genuinely differs by noise type, so it cannot be collapsed — agreement across strategies LogD 0.78 / hERG 0.76 / Caco-2 0.59, from `table_validation_auc_full.csv` (DISCUSSION_TRACKER.md:17). Validation data checked clean: 0 artifacts, 21 negative "collapse" cells real, 1 true NaN (DISCUSSION_TRACKER.md:18). **⚠ AVERAGING FLAG: the figure as it stands averages reps × 6 strategies (Panel A) and reps × strategies × 3 datasets (Panel B) — REVISION_STATUS.md:111–112. It hides that the "most robust model" changes with noise type and that magnitudes swing from −0.19 to +0.90 for one model (DISCUSSION_TRACKER.md:17).** |
| **D2** | Three tables were written but (supposedly) never cited. Stop emitting them, or keep them? | **DECIDED + IMPLEMENTED**, 2026-08-19 — DISCUSSION_TRACKER.md:31–36, 146 | (1) `table_validation_auc.csv` no longer emitted — author said "too much to report"; it is fully reconstructible from `table_validation_auc_full.csv`, max difference on the mean 2.2e-16 (DISCUSSION_TRACKER.md:22). (2) Rep-pooled `table4_uncertainty_metrics.csv` no longer emitted (DISCUSSION_TRACKER.md:33). (3) `table_validation_probabilistic.csv` **rebuilt** as one row per dataset × noise strategy at a single fixed representation (continuous PDV), 18 rows (DISCUSSION_TRACKER.md:34). Verified: every cell has exactly one underlying row, so nothing is averaged (DISCUSSION_TRACKER.md:36). **The "latent" premise was wrong for one of the three** — `table_validation_probabilistic.csv` IS Additional file 11, cited at paper L560 (DISCUSSION_TRACKER.md:23). **Residual open sub-item:** the v1 script mirror (DISCUSSION_TRACKER.md:146, REVISION_STATUS.md:147). |
| **D3** | A supplementary agreement table averages the six noise types together before computing its statistic. Keep it that way, or compute one per noise type? | **OPEN** (⬜ not discussed) — DISCUSSION_TRACKER.md:147, REVISION_STATUS.md:115 | — **⚠ AVERAGING FLAG: averages 6 strategies before the ICC; hides per-strategy disagreement.** |
| **D4** | A main results table carries mean/SD/mean-rank columns computed across the six noise types. Confirm keeping them. | **OPEN** (⬜) — DISCUSSION_TRACKER.md:148 | Provisionally judged acceptable because all six strategies are shown as columns beside the mean (REVISION_STATUS.md:117). **⚠ AVERAGING FLAG: the MEAN/STD/Mean_Rank columns are cross-strategy averages; they hide the 4× dose difference between strategies (immediate_next_steps.md:1170).** |
| **D5** | Add a table dumping each model's clean-data score for every representation and noise type. | **OPEN** (⬜) — DISCUSSION_TRACKER.md:149 | Not decided. Justification recorded: the value is already computed in memory but never written out (REVISION_STATUS.md:120); no QM9 table anywhere carries a clean-data score, so the whole "retention flatters weak models" argument is unconfirmable for QM9 (DISCUSSION_TRACKER.md:141). Blocked on a cluster run. |
| **D6** | Add tables relating clean accuracy to robustness, per noise type and per dataset. Roster: all representations, or the primary one only? | **OPEN** (⬜) — DISCUSSION_TRACKER.md:150 | Not decided. The only existing such analysis is a single correlation printed in a text box on one figure (REVISION_STATUS.md:121). |
| **D7** | Add a table with representation as the unit of aggregation. Roster: the ANOVA representation set, or all nine? | **OPEN** (⬜) — DISCUSSION_TRACKER.md:151 | Not decided. Recorded reason it matters: **no table anywhere uses representation as the aggregation unit**, so the paper's representation claim has no backing table (REVISION_STATUS.md:122). |
| **D8** | Fold the standalone analysis script into the main figure script and delete it; and settle the output-file naming. | **OPEN** (⬜) — DISCUSSION_TRACKER.md:152 | Not decided. Reason: it is not in any regeneration path, its numbers are uncertified, and its QM9 clean-data row is empty locally (REVISION_STATUS.md:123). |
| **D9** | Refit the validation ANOVA using the five cross-validation folds as repeats. Scope: one noise type only (to mirror QM9), or all six side by side? | **OPEN** (⬜) — DISCUSSION_TRACKER.md:153 | Not decided. The defect it fixes is confirmed: the loader averages the folds before integrating, leaving one observation per cell, which forces the unexplained-variance term to exactly zero (REVISION_STATUS.md:127; immediate_next_steps.md:395–398). Fix needs no re-run. |
| **D10** | Make the robustness calculation defend itself against catastrophically-diverged training runs, rather than relying on the caller to filter them. | **OPEN** (⬜) — DISCUSSION_TRACKER.md:154 | Not decided. Main pipeline is currently safe because the filter runs first (REVISION_STATUS.md:142); the corruption is confined to direct dumps that skip it. |
| **D11** | Should robustness be reported as *what fraction of accuracy a model keeps*, or as *how accurate it actually still is*, or both? | **DECIDED (metric)**, 2026-08-20; **three sub-questions still open** — DISCUSSION_TRACKER.md:155 | **Decided: report plain accuracy read at one noise level, σ = 0.6, and KEEP the retention metric alongside it** (author: "auc_norm needs to take into account baseline performance at higher sigmas. The r2 at sigma=0.6 is a bit of a sanity check on auc_norm"). Evidence chain: (i) retention and absolute accuracy rank models differently — the two are near-independent, rank agreement 0.179 across model means on 45 common cells (DISCUSSION_TRACKER.md:80); (ii) the σ choice went through four iterations — statistical separation → per-dataset label-SD matching → per-dataset failure thresholds → **fixed σ = 0.6, chosen by the author and then backed by published assay error 0.27–0.74 log units, clustered ~0.5, so σ=0.6 ≈ one unit of real experimental error** (DISCUSSION_TRACKER.md:125). **Still open: (b) the y-axis range of the robustness-vs-accuracy figure; (c) which validation results directory to use — one has 7 models, the other 13; (d) how the QM9 half is justified, since computed data has no experimental error to anchor to** (DISCUSSION_TRACKER.md:155). |

**Non-script items tracked alongside** (DISCUSSION_TRACKER.md:163–166): **R1** — actually run the script so the validation uncertainty table is emitted (it is wired but has never been written); **R2** — one cluster regeneration after D5–D8 land; **§8D** — the author's own paper edits.

### A2. REVISION_STATUS §8F — the ten author decisions (REVISION_STATUS.md:145–156)

| ID | Question | Status |
|---|---|---|
| 8F-1 | The over-averaged combined validation figure: re-facet by noise type, or relabel as an average and re-point the claims? | **OPEN** — same question as D1 (REVISION_STATUS.md:146) |
| 8F-2 | The three latent averaged tables | **RESOLVED** = D2 (REVISION_STATUS.md:147). Sub-item still open: the v1 regeneration script still calls the old script |
| 8F-3 | Fold in and delete the standalone analysis script; keep or rename its output prefix | **OPEN** = D8 (REVISION_STATUS.md:148) |
| 8F-4 | Compute the QM9 accuracy-vs-robustness correlation across all representations within each noise type, or on the primary one only? | **OPEN** = D6 (REVISION_STATUS.md:149). Notes the pooled +0.525 is an artefact and may appear only as a labelled pooled row. **⚠ AVERAGING FLAG** |
| 8F-5 | Representation × noise-type table: which representation roster? | **OPEN** = D7 (REVISION_STATUS.md:150) |
| 8F-6 | Validation ANOVA refit: one noise type or all six? | **OPEN** = D9 (REVISION_STATUS.md:151) |
| 8F-7 | Validation uncertainty is one-noise-type-only: accept with a limitations paragraph, or re-run? | **OPEN, with a recommendation to accept + caveat** (REVISION_STATUS.md:152) |
| 8F-8 | Self-guard the robustness calculation, or forbid direct dumps? | **OPEN** = D10 (REVISION_STATUS.md:153) |
| 8F-9 | Confirm three paper reframes: the SVM wording; one Bayesian comparison now being non-significant; "no neural net in the top ten" being false | **OPEN** (REVISION_STATUS.md:154) |
| 8F-10 | Run the cluster dump to certify the QM9 clean-data numbers, or cite them as provisional? | **OPEN** (REVISION_STATUS.md:155) |

Also in §8 but not numbered as decisions: **8A** the seven over-averaging sites (REVISION_STATUS.md:109–117), **8B** four analyses missing from the script (B1–B4, REVISION_STATUS.md:120–123), **8C** five validation problems (REVISION_STATUS.md:126–130), **8D** the paper-text edits already backed by current output (REVISION_STATUS.md:133–138), **8E** data/reproducibility (REVISION_STATUS.md:141–143).

### A3. immediate_next_steps §D — the analysis decisions

| ID | Question | Status | Decision + evidence |
|---|---|---|---|
| **D1** | What does the noise-level dial actually mean? | **DECIDED, NOT IMPLEMENTED** — immediate_next_steps.md:537–541, 1014 | **Define the level as the realised standard deviation of the injected noise, in label units, and renormalise each strategy so one unit means the same amount of corruption in all six.** Evidence: measured on the first 10,000 QM9 molecules, one unit of the dial injects 0.502 (outlier) to 2.000 (threshold) — a 4× span (immediate_next_steps.md:566–573). Precedent cited: Kolmar & Grulke 2021 *J Cheminform* 13:92 Eq. 7, and Heid et al. 2023 *JCIM* 63:4012 for matching mechanisms at equal SD (immediate_next_steps.md:546–554). Sub-decisions settled: don't truncate the grid, add a second reporting point at 0.9; report damage per dataset not pooled; keep zero as a permanent control; **do not** rescale by each dataset's label spread (immediate_next_steps.md:605–614). |
| **D1b** | The threshold strategy's cut-point: the paper says it applies to the standardised label, the code applies it to the raw one. | **DECIDED (fix the code), NOT IMPLEMENTED** — immediate_next_steps.md:616–644, 1015 | Fix the code, not the paper. Evidence: the code's raw cut hits 100.00 % of QM9 molecules and 83.8 % of LogD; the paper's standardised version hits 33.8 % and 33.2 % (immediate_next_steps.md:623–626). |
| **D1b-2** | Two more strategies also scale with the raw label — is that intended? | **OPEN** — immediate_next_steps.md:646–649, 1016 | — |
| **D2** | Cut some of the six noise strategies? | **DECIDED: keep all six** — immediate_next_steps.md:651–683 | A cut was drafted then withdrawn under adversarial checking, on four grounds: the redundancy evidence is QM9-only; at matched dose the two-cluster story collapses (quantile behaves like Gaussian, 1.1–1.2× damage, not like outlier at 2.1–3.0×); the redundancy criterion can't be applied consistently; and the most discriminating strategy is the one that would be cut. **But the presentational sub-question — how to show six strategies without six near-identical panels — is explicitly OPEN** (immediate_next_steps.md:683, 1051). |
| **D3** | Can the per-molecule uncertainty claim be rescued? | **PARTIALLY DECIDED** — immediate_next_steps.md:685–721, 1052 | Decided that as things stand it has no support (three reasons at :687–702) and that the population-level version is answerable and should be presented as such. **Whether it can be rescued by re-running is the live question**, answered by §N (protocol change + re-run), which is itself undecided. |
| **D4** | How to split uncertainty into its two components properly? | **PARTIALLY DECIDED** — immediate_next_steps.md:723–844, 1017 | The framing decided: the component rising with noise is the *sanity check*, not the anomaly the paper presents it as (Ryu/Kwon/Kim, *Chem Sci* 2019). The code change is specified in full (delete the stub, replace with a two-output network + Gaussian likelihood loss). **The go/no-go — whether to change the network at all — is flagged as DECISION NEEDED and must be made before any re-run** (immediate_next_steps.md:1017). *(Uncertainty workstream — noted, not analysed, per instruction.)* |
| **D5** | Which dead code is safe to delete? | **DECIDED** — immediate_next_steps.md:848–874 | Seven symbols in the Rust source verified dead and safe. **Six items verified NOT dead** and listed with reasons (immediate_next_steps.md:863–870). The document explicitly records that most of the original delete list "was wrong and would have broken the pipeline". |

### A4. immediate_next_steps §K — the ten decisions gating compute (immediate_next_steps.md:1240–1251)

All **OPEN**. K1 scope of the re-run · K2 do validation labels carry their own noise · K3 renormalise the dose · K4 move the threshold cut · K5 retire or re-run the per-molecule uncertainty question · K6 two-output head on the neural models · K7 keep or coarsen the noise grid · K8 run the skewed-noise arm or delete the broken samplers · K9 run the Gaussian process on the primary representation · K10 stop paying for six identical zero-noise runs.

### A5. immediate_next_steps §M — proposals (immediate_next_steps.md:1342–1393)

**M1** renormalise the dose per strategy · **M2** validation labels carry their own noise · **M3** scope of the re-run (cannot be costed until the cluster accounting command is run) · **M4** make the per-molecule uncertainty question answerable. **All ⬜ NOTHING DECIDED**, and the file explicitly retracts an earlier version that recorded M1 and M2 as author decisions (immediate_next_steps.md:1344–1346). Standing author instruction recorded at immediate_next_steps.md:1348–1351: *"the paper is fixed by RE-RUNNING, not by rewording."*

### A6. immediate_next_steps §H1 — thread register statuses worth surfacing

**DECISION NEEDED, unstarted:** A1b validation-into-training merge (:1008) · two-output head (:1017) · save uncertainty for training molecules (:1018) · skewed-noise experiment (:1020) · **which validation directory the script reads** (:1045) · figure y-axis zoom (:1063) · retire the dead v1 script pair (:1065).
**Blocked:** confirm the deployed cluster binary had the noise bug (:1007) · the whole QM9 re-run (:1030) · final figure selection (:1066).

### A7. DISCUSSION_REWORK — the author-facing questions (DISCUSSION_REWORK.md:149–158)

Three explicit asks, all **OPEN**: which of the five takeaways to build the paper around; whether the QM9-vs-experimental disagreement is a headline or a limitation; whether to add repeat training seeds for the neural models.

---

## B. KNOWN DEFECTS

### B1. Pipeline / code bugs — FIXED IN WORKING TREE (not committed, not re-run)

| Defect | Recorded at | State |
|---|---|---|
| QM9 held-out and validation splits were given the **training set's** noise map; test molecule *i* received training molecule *i*'s noise. Held-out labels corrupted; any per-molecule uncertainty correlation zero by construction | immediate_next_steps.md:20–48, :1006, :1133 | **Fixed in working tree, UNCOMMITTED.** Nothing re-run (:1497). Whether the *deployed* cluster binary had it is **unverified** (:1007, :1155) |
| Uncertainty writer appended one frame per fold to a filename with no fold in it → **only the last of five folds survived**; it also averaged over a within-fold row position, i.e. averaged different molecules together | immediate_next_steps.md:1567 | **Fixed in patched KIRBy** (:1560), not re-run. **Consequence: every existing uncertainty file is 20 % of the data** |
| The noise-level flag was never passed through for hERG | immediate_next_steps.md:1568 | Fixed in patch |
| The hERG loader hard-codes a column name the cached file does not have — raises on contact | immediate_next_steps.md:1569 | Fixed in patch; would have killed the cluster run |

### B2. Pipeline / code bugs — LIVE

| Defect | Recorded at |
|---|---|
| Four (elsewhere six) model families merge validation into training, so after the noise fix **11 % of training labels are now clean**. Both the before and after states are wrong; early stopping selects on clean labels, an oracle | immediate_next_steps.md:50–68, :1008, :1370–1383 |
| Error metrics are in standardised units whose scale moves with the noise level ⇒ **not comparable across noise levels** | immediate_next_steps.md:70–75, :1009 |
| The recorded-noise column is now identically zero on test rows; anything reading it is dead | immediate_next_steps.md:76–78, :1010, :1158 |
| The noise-strategy parameter file is **never passed to the binary** — dead since the day it was added | immediate_next_steps.md:79–101, :1011 |
| Three non-Gaussian noise-shape samplers are broken (one injects ~2.45× the nominal amount; one has support entirely below zero, so it shifts the mean) | immediate_next_steps.md:103–107, :209–213, :867, :1517 |
| Threshold cut applied to the raw label, contradicting the paper | immediate_next_steps.md:616–644, :1015 |
| Value-proportional and heteroscedastic also scale with the raw label — unintended floor | immediate_next_steps.md:646–649, :1016 |
| Two `continue` statements in the fingerprint block fire **after** part of a record is written → 256-byte-short record → **every later molecule read at the wrong offset**, silently swallowed by a bare except | immediate_next_steps.md:176–183, :1012, :1167 |
| Index drift between the noise map key and the target array. Does not fire on QM9; **live for the experimental sets** | immediate_next_steps.md:184–186, :1013, :1168 |
| One representation option desyncs the reader by 256 bytes per record | immediate_next_steps.md:869, :1022 |
| The scaffold noise strategy loads the scaffold map, discards it, and applies the training level to everything | immediate_next_steps.md:868, :1023 |
| The uncertainty-decomposition function the Bayesian nets actually call is a stub that hard-codes one component to nothing; the correct function has zero callers | immediate_next_steps.md:770–788, :1160 |
| One decomposition call site passes one argument to a two-argument function and unpacks two of three returns — **raises on contact, has never run** | immediate_next_steps.md:786–788 |
| A two-output head already exists but is disabled and its variance column is discarded | immediate_next_steps.md:1160 |
| Two models compute a real per-molecule noise term and throw it away | immediate_next_steps.md:1161 |
| The injected noise is **reconstructed by regression**, not recorded. At zero noise the leftovers are rounding error that grows with the label — which is where uncertainty is largest — so **the zero-noise control shows a *higher* correlation (0.080) than the real level does (0.057). The control fails in the direction that manufactures a positive result** | immediate_next_steps.md:688–697, :1421–1427 |
| The robustness calculation **averages across the ten replicates before integrating**, leaving one value per cell and no replicate spread — affects every ranking table and heatmap | immediate_next_steps.md:1137 (V5). **⚠ AVERAGING FLAG: hides run-to-run variance, which a Bayesian net alone moves by ~0.18 — about three times the entire between-model spread the QM9 ranking is built on (DISCUSSION_REWORK.md:98–99)** |
| **Two different clean-data cutoffs are in force under one headline** — 0.3 governs the ANOVA table, 0.6 governs the simple-effects table. Different configuration sets | immediate_next_steps.md:1164 |
| The methods figure plots synthetic data with two formulas that **do not match the injector**, while the paper implies it shows QM9 | immediate_next_steps.md:1165 |
| **The two injectors disagree**: the value-proportional constant is 0.1 in one and 0.05 in the other; the quantile strategy indexes in one and interpolates in the other. QM9 and the experimental sets were noised by different code | immediate_next_steps.md:1166, :1512 |
| The zero-noise condition is **run six times identically** for deterministic models ⇒ every zero-noise standard error and p-value is inflated sixfold | immediate_next_steps.md:1163 |
| Validation ANOVA residual is exactly zero — a saturation artefact from averaging folds before integrating. Script fix, no re-run | REVISION_STATUS.md:32, :74, :127; immediate_next_steps.md:395–398 |
| The catastrophic-run filter is **not random with respect to the question** — it biases unstable configurations' retention upward | immediate_next_steps.md:943 (INS-T2) |
| Two undeclared exclusion rules (runs below −0.5 deleted; ANOVA additionally drops below −10) and the decomposition is sensitive to them | immediate_next_steps.md:1178 |

### B3. Stale figures / dead scripts

| Defect | Recorded at |
|---|---|
| **The v1 figure script is the retired-metric (NDS) script — 216 mentions of it, zero of the current metric. `results/paper_figures/` is stale NDS output. Do not read numbers from it** | DISCUSSION_TRACKER.md:39 |
| The v1 regeneration shell script is still present and still tracked, and the project memory points at it — running it silently produces retired-metric tables | DISCUSSION_TRACKER.md:40; immediate_next_steps.md:1065 |
| Both validation-figure captions still say **NDS** | REVISION_STATUS.md:111–112 |
| The validation uncertainty table is wired into the script but **has never actually been written** — the earlier "27 rows DONE" was an in-memory spot check | REVISION_STATUS.md:128; DISCUSSION_TRACKER.md:164 |
| The second panel of the uncertainty figure has already been deleted from the script, so regenerating removes the only evidence for a claim still in the paper | DISCUSSION_REWORK.md:175 |
| No release binary exists in the tree; the cluster binary predates the noise fix | immediate_next_steps.md:1155 |

### B4. Data gaps

| Gap | Recorded at |
|---|---|
| Raw QM9 per-run files are **server-only**; zero such files exist locally, so QM9 clean-data and decoupling claims are assumptions | DISCUSSION_TRACKER.md:141; REVISION_STATUS.md:85, :141 |
| **No QM9 per-molecule uncertainty files exist anywhere**; the uncertainty figure's source data is experimental-only | immediate_next_steps.md:1172 |
| The Gaussian process was **never run on the paper's primary representation** for QM9; the figure script asks for it and gets nothing | immediate_next_steps.md:1162 |
| The Gaussian process on validation was only ever run on one representation, so it cannot enter a cross-representation ANOVA | immediate_next_steps.md:286–288; DISCUSSION_TRACKER.md:222 |
| Per-molecule uncertainty exists for **six validation files only**; nothing at all for hERG | immediate_next_steps.md:288–290 |
| Validation coverage: only 4 representations, 7 classic models, no Bayesian/variational models, one representation missing two models, hERG under-filled 153/156 | REVISION_STATUS.md:72 |
| The Gaussian process is absent from the local validation results table (outputs predate it being added) | REVISION_STATUS.md:129 |
| The clean-data carrier file is absent locally **and** has a corrupted column | REVISION_STATUS.md:84, :143 |
| The conformal arm directory contains no intervals, no coverage, no widths — nothing to analyse | DISCUSSION_REWORK.md:135–137; immediate_next_steps.md:296–297 |
| The 26 Gaussian-process re-run jobs (12822669–12822694) were submitted 2026-08-19; **outcome unverified** | DISCUSSION_TRACKER.md:234–270; immediate_next_steps.md:1024 |
| The standalone analysis script's QM9 clean-data row is empty locally | REVISION_STATUS.md:123 |
| No job output files exist anywhere in the repository — actual runtimes have never been recorded | immediate_next_steps.md:1265–1266 |
| The local Python environment cannot run the pipeline at all | immediate_next_steps.md:1332–1335 |
| One forest model cannot be fitted in the local environment (library version clash) — if it reproduces on the cluster every such job fails on contact | immediate_next_steps.md:1587 |

### B5. Wrong numbers / wrong claims in the paper itself

| Defect | Recorded at |
|---|---|
| The ANOVA table body in the paper is stale — the robustness half **matches no cell of current output** (e.g. heteroscedastic model term 37.0 → 14.0, residual 41.0 → 77.4). The prose above the table already quotes the correct numbers, so the paragraph contradicts its own table | REVISION_STATUS.md:29, :133; DISCUSSION_REWORK.md:168 |
| **Every value in the Wilcoxon table is wrong and one verdict flips** — one Bayesian-transformation comparison is p = 0.25, not significant, so "both transformations improved both networks" is three of four | REVISION_STATUS.md:136; DISCUSSION_REWORK.md:169; immediate_next_steps.md:909–910 |
| **NDS, the retired metric, still appears throughout including in the Conclusion where it is defined as *the* robustness metric.** ⚠ FLAG PER INSTRUCTION | DISCUSSION_REWORK.md:170 ("19 times"); REVISION_STATUS.md:34, :67 ("11 stale mentions") — **the two counts disagree** |
| ECE is defined and tabulated in the paper but has been deleted from the code, so those cells are unfillable | DISCUSSION_REWORK.md:170; immediate_next_steps.md:963; removal recorded DISCUSSION_TRACKER.md:50 |
| One representation is described as both the **most and the least** noise-robust, in the same section | DISCUSSION_REWORK.md:171; immediate_next_steps.md:964 |
| The exclusion cutoff is stated as 0.3 in four places and 0.6 in two; the live gate is 0.3 | DISCUSSION_REWORK.md:172; immediate_next_steps.md:965 |
| One additional file describes an artefact that was never computed; another's zero residual is a saturation artefact | DISCUSSION_REWORK.md:173 |
| "XGBoost and BNN variants suffer the most" cites a figure whose data **contains no rows for those models**; "NGBoost ranks first on external datasets" — it is third | DISCUSSION_REWORK.md:174 |
| The Methods claim that the noise dial is a consistent difficulty scale across strategies is **false** | DISCUSSION_REWORK.md:176; immediate_next_steps.md:599–601, :1170 |
| Methods say the uncertainty experiments were run once; most were run ten times | DISCUSSION_REWORK.md:177 |
| The paper and one additional file claim representation-specific SVM kernels; **the code uses a single kernel throughout** | DISCUSSION_REWORK.md:178; DISCUSSION_TRACKER.md:224; immediate_next_steps.md:961 |
| Paper L560 "QRF was consistently less robust than RF on every external data set" is an **averaging artifact** — that model is ahead on Caco-2 under four of six noise types | DISCUSSION_TRACKER.md:23; REVISION_STATUS.md:114. **⚠ AVERAGING FLAG: the cited table averaged 4 replicates × 6 strategies into 3 rows; it hid a 61/72 vs 11/72 split and a per-replicate spread of 0.366→0.622** |
| The second sentence at the same location ("representation has a minimal effect") is a separate averaging artifact | DISCUSSION_TRACKER.md:28. **⚠ AVERAGING FLAG** |
| Kendall's W is 0.92 in the paper, 0.9121 in the output; SVM is 5th, not top-2 | REVISION_STATUS.md:65, :135 |
| "No neural net in the top ten" is **false** under the current metric | REVISION_STATUS.md:137 |
| A duplicated sentence at paper L381 | REVISION_STATUS.md:68 |
| The paper presents the accuracy/robustness "decoupling" **as a finding** when it is the retention metric dividing out the baseline | DISCUSSION_TRACKER.md:80, :181; DISCUSSION_REWORK.md:47–61 |
| The claim that one model "maintained consistent [robustness] across all representations" | DISCUSSION_TRACKER.md:184 — **subsequently RESOLVED as NOT wrong**: the reversal was over-claimed, the apparent inconsistency was one divergent cell bleeding through raw aggregates; fix is the metric rename only (DISCUSSION_TRACKER.md:216) |

---

## C. NEEDS RE-GROUNDING — numbers asserted with no file behind them

**Every number in DISCUSSION_REWORK.md is in this category.** The file claims at :164 that "every item verified against the figure script's own output" but names a source file for exactly two things (the empty conformal directory at :135, and the Rust source at :41). Specifically ungrounded there:

- Per-unit dose figures and the −0.94 to −1.00 rank correlation (DISCUSSION_REWORK.md:21–23)
- "LightGBM wins on delivered accuracy 22 times out of 72 while never once winning on retention" (:56–57)
- "a Gaussian process on hERG scores 0.97 with a clean R² of −0.16" (:60) — *near-duplicate of immediate_next_steps.md:442 which says −0.156, also unsourced*
- "p = 0.25" for the Bayesian comparison (:79)
- "7 % to 18 % of runs collapse at zero noise" (:88–89)
- "A Bayesian network refit on identical data moves its own R² by about 0.18" (:98)
- "0.77 to 0.94 … no negative cell in 108 comparisons … 12 of those 108 disagree, one at −0.71" (:109–110)
- "a median of about 0.01 R², and it beats run-to-run noise in 1 of 18 cases" (:119–120)
- All Appendix A numbers: "37.0 → 14.0", "41.0 → 77.4", "NDS appears 19 times", "0.3 in four places and 0.6 in two" (:168–178)
- "Kolmar & Grulke use 6 figures of which only 4 carry results" (:182–183)

**DISCUSSION_TRACKER — file named but no line/column, or no file at all:**

- Mean clean-data score per representation: 0.583 / 0.572 / 0.558 / 0.548 (DISCUSSION_TRACKER.md:45) — **no file**
- "QRF wins 4/18 cells on continuous_pdv, 3/18 on mhggnn, 2/18 on ecfp4, 2/18 on sns" (:46) — **no file**
- The percentage-retained comparison "90.8 / 65.6 / 53.1" vs "90.8 / 82.1 / 86.1" (:106) — **no file**
- Separation figures "0.200 → 0.057", "0.169 → 0.102", "0.162" (:107) — **no file**
- The failure-threshold table, σ per dataset (:113–118) — described as "computed on the same gated set", **no file written**
- Per-strategy separation at σ=0.8: threshold 0.390 … hetero 0.063 (:77) — **no file**
- The whole "winner charts" block (:157–161) — **no file**. Also **⚠ AVERAGING FLAG: it explicitly notes reps "mostly agree" because "aggregating over models smooths the mirage"**
- The pattern-synthesis block (:171–177) — sourced only to a tool-results text file, `tool-results/bnk32v38d.txt`, not a pipeline output
- Kendall W across strategies 0.78 / 0.76 / 0.59 (:17) and the absolute-accuracy figures (:19) — file named at whole-file granularity only, or a scratchpad wildcard `scratchpad/absR2gated_*.csv`
- Model baselines "LogD (ECFP4 0.59/MHG 0.75/PDV 0.65/SNS 0.80)…" (:216) — traced to "raw KIRBy all_results.csv" at file granularity only

**REVISION_STATUS — the master findings table (REVISION_STATUS.md:40–53) is almost entirely unsourced:**

- F1 "max within-σ |ρ| = 0.129 across 143 combos; 0.485 was a pooling artifact" (:42) — **no file**
- F3 representation spreads 0.090 / 0.070 / 0.013 / 0.021 / 0.029 / 0.040 and "spread 0.22" (:44) — **no file**
- F4 "+0.30 / −0.31 / +0.10"; "server .out gave within-Gaussian +0.046" (:45) — **the .out file is not named**
- F5 "QRF 0.270, NGBoost 0.260, GP 0.184"; all the pooled correlation ranges (:46) — table named loosely as "table4, legacy"
- F6 every ECE value (:47) — **no file, and the metric has since been deleted from the code** (DISCUSSION_TRACKER.md:50)
- F8 "W = 0.9121" (:50) and F9 "baseline 0.857" (:51) — **no file**
- "dnn_bnn_full/morgan = 0.930 is #1" (:137) — **no file**
- "the 91 %/72 % numbers don't reproduce" (:138) — assertion, no file

**immediate_next_steps — mostly well-sourced; the exceptions:**

- The three retention values used to justify the reduced-grid model roster, 0.824 / 0.817 / 0.814 / 0.802 / 0.789 / 0.756 (:238–243) — **no file**
- "model choice explains 78 % of accuracy variance within PDV against 61 % within ECFP4" (:246–247) — restates :333 which *is* sourced to `table1_supp_simple_effects.csv`, but **⚠ AVERAGING FLAG: that table is explicitly "averaged over the six strategies" (:326)**
- "1.88 to 2.34, no peak" for discrimination flatness (:594) — **no file**
- "51 % of the σ = 1.0 drop" (:948) — **no file**
- The matched-dose damage ratios "2.1–3.0×" and "1.1–1.2×" (:495–497) — **computed, no file written**
- "0.868 and 0.99 on all three datasets" (:490) — **no file**
- The whole §I3 audit list: "0.398 to 0.156, in 27 of 27", "18 of 72", "0 of 24", "1.12 → 2.01", "0.635 to 0.884", "0.289 to 0.069", "18 of 30 clean runs below zero, median −0.288" (:1190–1196) — **no files named**
- Published assay-error values 0.54 / 0.27–0.62 / 0.43 (:588, :946) — cited to publications, but **one of these, the 0.27 figure, FAILED verification and is marked "do not use" at DISCUSSION_TRACKER.md:125 — yet it is still quoted as a live range in immediate_next_steps.md:588 and :946**
- The literature-workflow numbers (DISCUSSION_TRACKER.md:125) trace to `tasks/wqrlyif68.output`, and **verbatim quotes are explicitly NOT yet available — "cite, do not quote"** (:129)

---

## D. CONTRADICTIONS BETWEEN THESE DOCUMENTS

**Already catalogued by the author** (immediate_next_steps.md:1117–1124):

| # | Contradiction | Where |
|---|---|---|
| X1 | Two guide threads say KEEP the "robustness is decoupled from accuracy" claim and cite a specific model as evidence; guide §9.1 says that claim is a metric artefact and citing that model is citing the artefact. Both are written into the same file | `REVISION_GUIDE.md` L62–134 vs §9.1, per immediate_next_steps.md:1121 |
| X2 | One thread treats "threshold noise is inert on QM9" as a **finding**; another treats it as a **code bug to be fixed** | immediate_next_steps.md:122 vs :616–644; noted at :1122 |
| X3 | Which validation directory is canonical — the tracker asserts both, each with evidence | DISCUSSION_TRACKER.md:41 ("alternative_full IS correct for the loader", supersedes the earlier claim) vs :220 ("Canonical validation data = validation_rerun … alternative_full = a different endpoint entirely"); noted at immediate_next_steps.md:1123 |
| X4 | One document says the variational models are fine (a scale artefact); another says the low coverage despite wide intervals is **not** only a units artefact | REVISION_STATUS.md:76 vs DISCUSSION_TRACKER.md:53; noted at immediate_next_steps.md:1124 |

**Additional contradictions not in that list:**

| # | Contradiction | Where |
|---|---|---|
| X5 | **Row count for the same validation directory**: 28,710 rows vs 48,510 rows vs 16,170 rows per dataset — and three *different* directory paths are each described as the complete one | DISCUSSION_TRACKER.md:220 vs :58 and immediate_next_steps.md:280 vs :1157 |
| X6 | **The value-proportional constant.** One document says "the paper uses 0.1; the code uses 0.05" as a defect; the other says the paper's 0.1 is **correct** because it is the Rust default and the 0.05 file was never read — and explicitly instructs that the "code uses 0.05" note **must be deleted** | DISCUSSION_REWORK.md:176 vs immediate_next_steps.md:96–99 and :945. *(Partially reconciled by immediate_next_steps.md:1166: both constants are real, in two different injectors)* |
| X7 | **Does the noise level affect how well models separate?** The tracker's σ-selection table shows separation rising monotonically from 0.068 to 0.257 across the grid and recommends 0.8 on that basis; the later document states separation is **flat across the whole grid, no peak**, and warns "do not manufacture a data-driven justification the data does not support" | DISCUSSION_TRACKER.md:62–77 vs immediate_next_steps.md:593–597 |
| X8 | **How often the two metrics pick the same best model**: "20 of 72 cells (28 %)" vs "18 of 72" — **within the same file** | immediate_next_steps.md:438 vs :1193 |
| X9 | **Per-molecule spread of two strategies**: "vary their per-molecule noise by under 10 %, so they're near-homoscedastic" vs "2.2×" — the second file explicitly logs the first as an error to be corrected | DISCUSSION_REWORK.md:30 vs immediate_next_steps.md:116–117 and :958 |
| X10 | **How many strategies genuinely concentrate noise**: "only outlier and quantile genuinely corrupt a subset" vs "at matched dose quantile behaves like Gaussian … the concentrated mechanism has one member, not two" | DISCUSSION_REWORK.md:30 vs immediate_next_steps.md:494–498 |
| X11 | **Is the retention metric the sole metric?** One says it is the sole metric, no alternatives; the other decides accuracy-at-one-noise-level becomes the ranking instrument with retention kept as a secondary descriptor | REVISION_STATUS.md:15 vs DISCUSSION_TRACKER.md:155 |
| X12 | **Is one model genuinely the most robust?** One lists that as a holding finding; the other says it tops retention only because it has the lowest clean-data score and is "retention-flattered" | REVISION_STATUS.md:50 vs DISCUSSION_TRACKER.md:173, :185 and DISCUSSION_REWORK.md:54–57 |
| X13 | **Which model is the "retention mirage"** — asserted as one model, then corrected to a different one in the same file, with the original claim left standing above the correction | DISCUSSION_TRACKER.md:19 vs :79 |
| X14 | **ECE**: reported as a live finding with six values in one document; recorded as fully deleted from paper and all three scripts in another | REVISION_STATUS.md:47 vs DISCUSSION_TRACKER.md:50 |
| X15 | **Validation uncertainty coverage**: "3 datasets × 4 reps, models QRF+NGBoost everywhere, GP on PDV only … 27 rows DONE" vs "per-sample uncertainty exists for six files only … nothing for hERG" — and a third statement that the 27-row claim was an in-memory spot check, never actually emitted, and a fourth that every such file is one fold of five | REVISION_STATUS.md:71 vs immediate_next_steps.md:288–290 vs REVISION_STATUS.md:128 vs immediate_next_steps.md:1567 |
| X16 | **Count of retired-metric occurrences in the paper**: 19 vs 11 | DISCUSSION_REWORK.md:170 vs REVISION_STATUS.md:34 |
| X17 | **Target float count**: "6 figures + 4 tables" vs "Five main figures, four main tables" | DISCUSSION_REWORK.md:184 vs immediate_next_steps.md:1206 |
| X18 | **The combined validation figure**: one document says delete it outright; the tracker still has its fate as an undecided question with two live options | DISCUSSION_REWORK.md:191–192 vs DISCUSSION_TRACKER.md:145 |
| X19 | **Whether one model is rep-dependent**: asserted as a must-change paper error, then resolved in the same file as over-claimed and explicitly "do NOT reverse" | DISCUSSION_TRACKER.md:184 vs :216 |
| X20 | **Cost of the QM9 re-run**: "11,132 requested wall-hours" reported as a fact, then immediately reframed as "a request, not a cost" with a different total (19,615 over all scripts) and the note that actual runtimes have never been recorded | immediate_next_steps.md:1156 vs :1258–1266 |
| X21 | **The 0.27 assay-error figure** is marked FAILED VERIFICATION / do-not-use in one document and still quoted as a live range endpoint in another | DISCUSSION_TRACKER.md:125 vs immediate_next_steps.md:588, :946 |

---

## E. WHAT THE AUTHOR WAS STILL WRESTLING WITH AT THE POINT OF STOPPING

Ordered by how load-bearing each is. Latest timestamps: DISCUSSION_TRACKER 2026-08-21 (job note 2026-08-24), immediate_next_steps 2026-08-21 with §M/§P dated 2026-08-23, DISCUSSION_REWORK 2026-08-20, REVISION_STATUS 2026-08-17.

1. **How big the re-run should be, and whether it can be afforded at all.** K1/M3 — the headline cost figure was found to be a sum of requested time limits, not a measurement, and the one command that would give the real number has not been run (immediate_next_steps.md:1242, :1268–1276, :1385–1389).
2. **Whether to redefine what one unit of the noise dial means** — renormalise all six strategies to a common dose (M1/K3). Decided in §D1, explicitly *not* decided in §M, and it gates the threshold fix and the grid decision (immediate_next_steps.md:1014, :1353–1368, :1236).
3. **Whether the validation split should carry its own noise** (M2/K2/A1b). Flagged DECISION NEEDED, blocks every fitted model in both pipelines, and blocks a Methods thread (immediate_next_steps.md:1008, :1243, :1370–1383, :1098).
4. **Which validation results directory is canonical** — 7 models or 13. Unresolved contradiction X3, and it directly blocks the σ=0.6 figure work (DISCUSSION_TRACKER.md:136–137, :155(c); immediate_next_steps.md:1045).
5. **Whether the per-molecule uncertainty question is retired or re-run** (K5/M4/§N). A full protocol and job set have been built and tested but nothing submitted; three sub-questions remain (how many cross-fitting folds, whether validation molecules are scored, whether every probabilistic model is included) — immediate_next_steps.md:1246, :1479–1483, :1553.
6. **Whether to change the neural networks to emit a per-molecule noise estimate** (K6/D4-head) — must be settled before any re-run starts (immediate_next_steps.md:1017, :1247, :812–813).
7. **How to present six noise strategies without six near-identical panels.** Explicitly OPEN after the decision *not* to cut any (immediate_next_steps.md:683, :1051).
8. **Which of the five takeaways the paper is built around**, and whether the QM9-vs-experimental disagreement is a headline or a limitation (DISCUSSION_REWORK.md:151–157).
9. **Whether repeat training seeds are added for the neural models** — without them the Bayesian comparisons are stated to be uninterpretable (DISCUSSION_REWORK.md:99–100, :157–158).
10. **The remaining three sub-questions under the metric decision**: the robustness figure's y-axis range; how the QM9 half of the σ=0.6 justification is framed given computed data has no assay error (DISCUSSION_TRACKER.md:155, immediate_next_steps.md:1063).
11. **Whether to delete or archive the retired-metric script pair** (DISCUSSION_TRACKER.md:40; immediate_next_steps.md:1065).
12. **The seven unresolved script-change decisions D3–D10**, all still ⬜ never discussed, which between them block the QM9 clean-data dump, the representation table, and the validation ANOVA refit (DISCUSSION_TRACKER.md:147–154).
13. **Whether the skewed-noise arm runs at all, or the three broken samplers are simply deleted** (K8/B2c, immediate_next_steps.md:1020, :1249).
14. **Whether the 26 Gaussian-process jobs submitted on 2026-08-19 ever finished** — the last note in any of these documents (2026-08-24) is a partition-juggling entry saying none had completed at check time (DISCUSSION_TRACKER.md:270).