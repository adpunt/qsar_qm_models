# INVENTORY: FIGURES, TABLES, ADDITIONAL FILES — `/Users/apunt/repos/qsar_qm_models/paper.tex` + `/Users/apunt/repos/qsar_qm_models/additional_files.tex`

## COUNTS
- Main-text figures: **8** (6 × `\begin{figure}`, 2 × `\begin{figure*}`) — `paper.tex` L356, L406, L415, L424, L486, L533, L544, L553
- Main-text tables: **6** — `paper.tex` L266, L318, L385, L435, L468, L501
- Additional files: **12** declared (`paper.tex` L659–L670), all 12 realised in `additional_files.tex` (L38, L126, L208, L268, L309, L338, L354, L370, L395, L424, L440, L465)

---

## A. MAIN-TEXT TABLES

| # | label | paper.tex line | content | supposed to show | body refs (line) | caption metric == body? | stale NDS in caption? | what is averaged | distinct facts | VERDICT |
|---|---|---|---|---|---|---|---|---|---|---|
| T1 | `tab:metrics_summary` | L266 (cap L305, label L309) | 9-row metric glossary: RMSE, R², AUC_norm, Wilcoxon, Kendall W, ANOVA η², ICC, Spearman ρ, Coverage, **ECE** | definitions of every metric used | **NEVER CITED IN BODY** (no `\ref{tab:metrics_summary}` anywhere) | n/a — no body text to compare | No (uses AUC_norm at L279–281) — but lists **ECE** (L300–302), which no longer exists in the pipeline (`grep -c "ece\|ECE" scripts/generate_paper_figures_v2.py` → **0**) | nothing (no data) | 0 results-facts; it is a definitions list | **keep-but-fix**: must be cited in text or moved to Methods prose; delete the ECE row |
| T2 | `tab:regression_noise` | L318 (cap L344, label L349) | 6 noise strategies × per-point scale sᵢ × simulated source | how each strategy scales noise | L222, L308 (own caption) | yes | no | nothing | 6 facts (one formula per strategy) — genuinely dense, earns its place | **keep-as-is** (see Note 6 re: threshold cut-point) |
| T3 | `tab:anova_decomposition` | L385 (cap L387, label L388) | 6 strategies × {Perf η² Model/Rep/Inter/Resid} × {Robust η² same 4} | variance attribution for performance and for robustness | L380 | **NO — caption L387 says "Robustness uses the noise degradation slope (NDS)"; body L380 says AUC_norm** | **YES — caption L387 explicitly says NDS** | η² is computed over 10 replicates as the error term (`paper.tex` L260); no strategy averaging (rows are per strategy) — good | 48 numbers, ~3 facts: (i) interaction dominates performance, (ii) model dominates robustness for 4 strategies, (iii) residual dominates for outlier+hetero | **keep-but-fix — URGENT: the whole Robustness half is STALE, see Note 1** |
| T4 | `tab:auc_ranking` | L435 (cap L437, label L438) | 11 models × 6 strategies AUC_norm on PDV + Mean | robustness ranking of models | L433, L460 | yes | no | **Mean column averages over 6 strategies**; each cell is a mean over 10 replicates. Hides that SVM, not NGBoost, wins Outlier and that outlier spread is 0.02 vs threshold 0.13 | ~2 facts: NGBoost/RF top, NN-β bottom; strategy ordering barely changes ranks | **keep-but-fix**: verified current (matches `results/paper_figures_v2/table2_auc_by_strategy_pdv.csv` rows 2–12 cell-for-cell); drop or asterisk the Mean column as a cross-strategy average |
| T5 | `tab:wilcoxon_bnn` | L468 (cap L470, label L471) | 5 paired tests, Δ metric + p + significance | do Bayesian transforms help | L466 | **NO — column header L475 is "Δ NDS"; the regenerated quantity is Δ AUC_norm** | **YES — caption L470 twice, header L475** | Δ is a mean change across the paired configurations (n=24–44) | 2 facts: BNN helps NNs; QRF < RF | **keep-but-fix — URGENT: numbers stale AND one result flips, see Note 2** |
| T6 | `tab:top_unc_noise` | L501 (cap L503, label L504) | top-10 + bottom-4 model×rep, Unc-Noise ρ / Unc-Error ρ / **ECE** / Cov 1σ / 2σ | which pairings track injected noise | L499, L531, L540 | partially — see Note 5 | no | pooled across σ (see Note 5) | ~2 facts: GP/NGBoost top, embeddings collapse | **keep-but-fix** (uncertainty workstream owns the substance; the ECE column is unregenerable — `ECE` count 0 in the v2 script) |

---

## B. MAIN-TEXT FIGURES

| # | label | paper.tex line | image file | supposed to show | body refs (line) | caption==body? | stale NDS? | what is averaged | distinct facts | VERDICT |
|---|---|---|---|---|---|---|---|---|---|---|
| F1 | `fig:noise_strategies` | L356 (img L358, cap L359, label L361) | `fig_methods_noise_strategies.png` | 6 panels: clean vs noisy QM9 gap label histogram at σ=0.5, RMSE annotated | L354 | yes | no | nothing averaged (label distributions) | 6 facts (per-strategy perturbation shape + RMSE) | **keep-as-is** — highest information-per-inch figure in the paper |
| F2 | `fig:anova_decomposition` | L406 (img L408, cap L409, label L410) | `fig2_anova_decomposition.png` | stacked η² bars, a) performance b) robustness, by strategy | L380, L383 | yes (caption says AUC_norm) | no | none across strategies (bars are per strategy); η² over 10 replicates | **0 new facts** — it is T3 redrawn | **merge-with-T3**: pick one. Recommend keep the figure, cut the table (or vice versa) — carrying both is pure duplication |
| F3 | `fig:interaction` | L415 (img L417, cap L418, label L419) | `fig_interaction.png` | a) AUC_norm heatmap model×rep (Gaussian); b) AUC_norm ECFP4 vs PDV scatter + Spearman | L413 | yes | no | **panel a: mean over 10 replicates** (`generate_paper_figures_v2.py` L3791 `aggfunc='mean'`); **panel b: mean over replicates** (L3823–3824 `.groupby('model')['auc_norm'].mean()`). Hides replicate variance — which the ANOVA says is 83.6%/77.4% of robustness variance for outlier/hetero | 2 facts: SVM/BNN flat across reps; ρ≈0.8 between reps | **keep-but-fix**: the ρ=0.82, p=0.002 quoted at L413 is drawn on the PNG only — **NO SOURCE FOUND** in any `results/paper_figures_v2/*.csv` |
| F4 | `fig:global_overview` | L424 (img L426, cap L427, label L428) | `fig1_global_overview.png` | a) R² vs σ curves (PDV, Gaussian, ~7 models); b) AUC_norm heatmap model×strategy (PDV) | L422 | yes | no | **panel a: mean R² over 10 replicates** (script L2666 `.groupby('sigma')['r2'].mean()`); panel b `aggfunc='mean'` over replicates (script L2685) | panel b is a picture of T4 → **1 new fact** (panel a's curve shape) | **merge-with-T4 / demote panel b**: panel b and Table 4 are the same 66 numbers. Keep panel a, cut panel b or cut T4 |
| F5 | `fig:nn_family_comparison` | L486 (img L488, cap L489, label L490) | `fig_nn_family_comparison.png` | R² vs σ, PDV+Gaussian, a) NN-α family b) NN-β family c) RF vs QRF | L466 | yes | no | mean over 10 replicates | **1 fact** ("Bayesian variants sit above their deterministic base; QRF sits below RF") — which T5 already states with p-values | **merge-with-T5 or cut.** Also: caption L489 says *"Heteroscedastic results (not shown) are qualitatively consistent"* — direct violation, see §D |
| F6 | `fig:uncertainty_combined` | L533 (img L535, cap L536, label L537) | `fig_uncertainty_combined.png` | a) mean predicted uncertainty vs σ (PDV, Gaussian); b) aleatoric vs epistemic vs σ for VBLL | L531 | yes | no | **means over all test samples at each σ** (script L3175–3178), PDV+Gaussian only | 2 facts | **keep-but-fix** (uncertainty workstream) |
| F7 | `fig:validation_overview` | L544 (`figure*`, img L546, cap L547, label L548) | `fig_validation_overview.png` | 3 stacked AUC_norm heatmaps, model×strategy, per external dataset | L551 | **NO — body L551 calls them "Per-dataset NDS heatmaps"; the figure is AUC_norm** | body text is stale-NDS; caption is clean | mean over CV folds; per-strategy (no strategy averaging) ✓ | 3 facts (one ranking per dataset) | **keep-but-fix**: caption omits that the figure is **PDV-only** (`generate_paper_figures_v2.py` L1508 `val_auc_primary = val_auc_df[val_auc_df['rep'] == PRIMARY_REP]`); the PNG carries it in a suptitle the LaTeX caption never states |
| F8 | `fig:validation_combined` | L553 (`figure*`, img L555, cap L556, label L557) | `fig_validation_combined.png` | a) grouped AUC_norm bars model×dataset (incl. QM9); b) QM9 vs external scatter | L551 | **NO — caption L556 says "noise degradation slope (NDS) … lower (more negative) NDS"; the plotted axis is AUC_norm** (script L1749 `ax_a.set_ylabel('AUC$_{norm}$')`, L1768–1769 axes labelled "Mean AUC_norm") | **YES — caption L556, twice** | **WORST OFFENDER: triple average.** Panel A `val_auc_df.pivot_table(values='auc_norm', index='model', columns='dataset', aggfunc='mean')` (script L1715) collapses **6 strategies × all representations × all CV folds** into one bar; the QM9 column (script L1719) collapses **6 strategies × all reps × 10 replicates**. Panel B (script L1730–1732) does the same on both axes. This hides the entire outlier-vs-threshold spread (0.02 vs 0.13, from `table2_auc_by_strategy_pdv.csv`) and every rep effect | 1 fact ("ranking transfers, XGBoost doesn't") | **replace.** As drawn it is exactly the averaging the analysis plan forbids. Replace with a per-strategy small-multiple, or cut and let F7 + the validation ANOVA carry it |

### Representation / strategy restriction of each main-text figure

| figure | representation | noise strategy | does the paper justify the restriction? |
|---|---|---|---|
| F1 noise_strategies | n/a (labels only) | all 6 shown | n/a |
| F2 anova_decomposition | all reps in ANOVA roster | all 6 | yes (design stated L250, L262–264) |
| F3 interaction | ECFP4, PDV, SMILES, mol2vec, MHG-GNN | **Gaussian only** | **NO justification anywhere in paper.tex** |
| F4 global_overview | **PDV only** | all 6 in panel b; **Gaussian only** in panel a | stated at L422 ("on the PDV representation"), **not justified** |
| F5 nn_family | **PDV only** | **Gaussian only** | **NO** — instead L489 waves at hetero as "not shown" |
| F6 uncertainty_combined | **PDV only** | **Gaussian only** | **NO** |
| F7 validation_overview | **PDV only** (undisclosed in caption) | all 6 | **NO** — the justification exists only as a source comment (`generate_paper_figures_v2.py` L1502–1505), never in the manuscript |
| F8 validation_combined | **all reps averaged together** | **all 6 averaged together** | **NO** |

The manuscript never anywhere argues *why* PDV and Gaussian are the defaults. The strongest available argument (representation η² for robustness is small) exists in the code comment at `scripts/generate_paper_figures_v2.py` L1502–1505 and is supported by `results/paper_figures_v2/table1_anova_summary.csv` (Robust_Rep_η² = 0.22–7.89% across all six rows) — it is simply not in the paper.

---

## C. ADDITIONAL FILES

| AF | additional_files.tex line | content | first cited (paper.tex) | stale NDS in caption? | averaged over | facts | VERDICT |
|---|---|---|---|---|---|---|---|
| 1 | L38–118 | hyperparameter longtable, 11 models | L197, L208 | no | n/a | reference material | **keep-as-is** |
| 2 | L126–200 | 58 model-pair Spearman ρ ≥ 0.95 | L264 | **YES** ("model noise degradation slope (NDS) profiles", L130) | ρ computed over model profiles (n=16–52 configs) | 1 fact (which pairs were dropped) | **keep-but-fix**: relabel NDS→AUC_norm; regenerate from `table_supp_model_redundancy.csv` |
| 3 | L208–260 | 36 representation-pair ρ | L264 | **YES** (L212) | as above | 1 fact | **keep-but-fix** (source: `table_supp_rep_redundancy.csv`) |
| 4 | L268–301 | ICC(1,1), 19 model pairs, incl. "Mean \|ΔNDS\|" column | L264 | **YES** (L272 twice, and the column header L278) | ICC over n=3–8 configs | 1 fact | **keep-but-fix** (source: `table_supp_icc.csv`) |
| 5 | L309–330 | excluded-configuration table | L247, L422 | no NDS, but **wrong threshold and wrong contents** | count of strategies | 1 fact | **keep-but-fix — URGENT, see Note 3** |
| 6 | L338–346 | `fig1_supp_ecfp4_overview.png` | L433 | **YES** ("Noise degradation slope (NDS) heatmap", L344) | mean over replicates | 1 fact ("ECFP4 looks like PDV") | **keep-but-fix** caption metric |
| 7 | L354–362 | `fig3_ranking_consistency.png` | L433 | **YES** (L359, L360) | none — per-config points | 1 fact (baseline R² ⊥ AUC_norm) | **keep-but-fix**: figure is baseline R² vs **AUC_norm** (script L2919–2925, y-label `AUC$_{norm}$`), not NDS |
| 8 | L370–387 | Bayesian-transform effect, only 3 rows, only NN-β | L466 | **YES** (L374, and 3 of 6 column headers L380) | Δ is a per-rep mean | 1 fact | **keep-but-fix**: body L466 promises "choice of transformation varies by representation" for *both* families; the file shows NN-β only, 3 reps |
| 9 | L395–416 | uncertainty metrics, ECFP4, 7 models, incl. ECE | L531 | no | pooled over σ | uncertainty workstream | **keep-but-fix** (ECE column unregenerable) |
| 10 | L424–432 | `fig_validation_anova.png` | L551 | **YES** ("noise degradation slope (NDS) variance", L430) | legacy/Gaussian strategy only (script L1639 comment) | 1 fact | **keep-but-fix**: `results/paper_figures_v2/table_validation_anova.csv` rows 2–4 give Residual_η² = **0.0** on all three datasets with n_reps=3 — a zero residual means no replicate error term survived; caption must not claim a variance decomposition without disclosing that |
| 11 | L440–457 | RF vs QRF, 3 datasets | L560 | **YES** (L444, L445, and all 3 column headers L450) | **each value averages over all 6 strategies and all reps** | 1 fact | **keep-but-fix**; note memory-flagged concern that "QRF consistently less robust" is an averaging artefact |
| 12 | L465–490 | validation hyperparameters, tuned SVM kernels | L197 | no | n/a | reference | **keep-as-is** |

---

## D. NOTES — the load-bearing problems

**Note 1 — Table `tab:anova_decomposition` (L390–404) contradicts both the regenerated data and its own body paragraph.** The Performance half matches `results/paper_figures_v2/table1_anova_summary.csv`; the Robustness half does not.

| Strategy | paper.tex Robust Model/Rep/Inter/Resid | `table1_anova_summary.csv` (row) |
|---|---|---|
| Gaussian (L396) | 48.7 / 8.2 / 11.9 / 31.2 | 43.77 / 5.15 / 16.92 / 34.15 (row 4) |
| Quantile (L397) | 47.9 / 6.8 / 14.9 / 30.4 | 36.79 / 4.42 / 15.10 / 43.70 (row 5) |
| Threshold (L398) | 48.0 / 10.1 / 28.0 / 13.9 | 54.68 / 7.89 / 22.58 / 14.85 (row 7) |
| Heteroscedastic (L399) | 37.0 / 4.5 / 17.5 / **41.0** | **13.99 / 0.68 / 7.95 / 77.38** (row 3) |
| Value-prop. (L400) | 52.8 / 6.9 / 22.2 / 18.2 | 52.54 / 6.00 / 19.90 / 21.56 (row 2) |
| Outlier (L401) | 12.7 / 3.2 / 4.7 / 79.3 | 10.29 / 0.22 / 5.86 / **83.64** (row 6) |

Body L380 quotes "36.8–54.7%" (= CSV min 36.79 Quantile, max 54.68 Threshold ✓) and "83.6% and 77.4%" (= CSV 83.64, 77.38 ✓). So the **prose is current and the table it points at is stale**, and the table's own bolding (L399 bolds Hetero Model=37.0 as dominant) asserts the opposite of the sentence that cites it.

**Note 2 — Table `tab:wilcoxon_bnn` (L473–483): one significance result flips.** Against `results/paper_figures_v2/table3_wilcoxon_tests.csv`:

| row | paper.tex Δ / p / sig | CSV `improvement` / `p_value` / `significant` |
|---|---|---|
| NN-α → BNN-α (L477) | +0.056 / 2.9e-10 / * | 0.0310 / 2.910e-11 / True (row 2) |
| **NN-α → VBLL-α (L478)** | **+0.061 / 1.2e-6 / \*** | **0.0113 / 0.2522 / False** (row 3) |
| NN-β → BNN-β (L479) | +0.096 / 2.9e-11 / * | 0.0526 / 2.910e-11 / True (row 4) |
| NN-β → VBLL-β (L480) | +0.124 / 1.2e-7 / * | 0.0618 / 1.192e-7 / True (row 5) |
| RF → QRF (L481) | −0.022 / 1.6e-10 / * | −0.0117 / 2.876e-11 / True (row 6) |

Body L466 ("Both full BNN and VBLL transformations significantly improved robustness for both NN-α and NN-β") and Conclusion L577 are therefore **not supported** for the NN-α/VBLL-α pair by the regenerated data.

**Note 3 — Additional file 5 (L317–329) is wrong about both the threshold and the contents.** `results/paper_figures_v2/paper_figures_report.txt` (tail) states: `Robustness baseline R² gate: 0.3` and `Excluded 48 configurations (baseline R² < 0.3)`. `results/paper_figures_v2/excluded_configs.csv` has exactly 48 data rows, and they are exclusively {BNN-α, BNN-β, VBLL-α, VBLL-β} × {MHG-GNN, mol2vec} × 6 strategies. The regenerated supplement `results/paper_figures_v2/table_supp_excluded_configs.tex` shows precisely that 4×2 layout. The additional_files.tex version instead shows SMILES and Rand.SMILES columns and RF/QRF/NGBoost rows summing to 66 — pure v1 residue. Compounding this, the threshold is quoted three different ways in the manuscript: **R² < 0.3** (paper.tex L247, L422), **R² < 0.6** (paper.tex L464), **R² ≤ 0.6** (paper.tex L663, additional_files.tex L313).

**Note 4 — `additional_files.tex` L24 sets `\graphicspath{{results/paper_figures/}}`, the STALE v1 NDS output directory.** All three additional-file figures (L342 `fig1_supp_ecfp4_overview.png`, L358 `fig3_ranking_consistency.png`, L428 `fig_validation_anova.png`) exist in that directory, so the file compiles silently while embedding v1 NDS graphics. It must point at `results/paper_figures_v2/`.

**Note 5 (uncertainty workstream — one line, flagged only because it collides with the metric definition):** Methods L226 says the uncertainty–noise correlation "was computed within each fixed noise level σ", but Table `tab:top_unc_noise` quotes the *pooled* column — `results/paper_figures_v2/table4c_top_unc_noise_correlations.csv` header column 3 is `Unc-Noise ρ (pooled)` (NGBoost/SNS 0.4722, NGBoost/PDV 0.4720, matching paper L512–513's 0.47), while the within-σ columns for those same rows are 0.076/0.087 and 0.018/0.023.

**Note 6 — unverifiable numbers in the body (no source in `results/paper_figures_v2/`):**
- Kendall's W = 0.9121, p = 3.55e-8 (L433) and W = 0.92 (L573) — the v2 script writes `table6_kendalls_w.txt` (`generate_paper_figures_v2.py` L3743), but **no such file exists** in `results/paper_figures_v2/`. **NO SOURCE FOUND.**
- Spearman ρ = 0.82, p = 0.002 for ECFP4 vs PDV (L413) — computed inside the figure function (`generate_paper_figures_v2.py` L3843) and written only onto the PNG. **NO SOURCE FOUND** in CSV.
- "For SMILES, model choice explains over 91% of robustness variance, while on PDV, model choice explains 72%" (L493) — `results/paper_figures_v2/table1_supp_simple_effects.csv` gives, for `Robustness / Model effect`, SMILES = 87.80 (Gaussian, row 22), 83.36 (Quantile, row 54), 22.93 (Outlier, row 86), 93.86 (Threshold, row 118), 92.79 (Value-Prop, row 150), 71.65 (Hetero, row 182); PDV = 58.92 / 55.67 / 18.74 / 68.94 / 62.56 / 35.57 (rows 18, 50, 82, 114, 146, 178). **Neither 91% nor 72% matches any single strategy** — if it is a cross-strategy average it must be labelled as one, and the SMILES average is not 91.
- "representation explains less than 11% of NDS variance (p > 0.25)" (L464) — nearest source is `table1_supp_simple_effects.csv` row 24, Gaussian / Robustness / Rep effect / `dnn_bnn_full` = 10.11, **p = 0.2373 (not > 0.25)**; the companion `mlp_bnn_full` on the same strategy (row 28) is **33.93, p = 0.0056**, contradicting the "less than 11%" claim for full-BNNs generally.
- Body L433 "NGBoost and random forests retained the most predictive performance across all strategies" is contradicted by the paper's own Table `tab:auc_ranking` L448, where SVM (0.956) leads Outlier over RF (0.955) and NGBoost (0.953) — confirmed in `table2_auc_by_strategy_pdv.csv` (svm/Outlier 0.955555, rf 0.954834, ngboost 0.952861) and `table2_auc_ranks_pdv.csv` row 5 (svm Outlier rank 1.0).
- The "spread of 0.02 / 0.13" claim (L460) **does** check out: `table2_auc_by_strategy_pdv.csv` Outlier max 0.955555 − min 0.932218 = 0.0233; Threshold max 0.644772 − min 0.515977 = 0.1288.

**Note 7 — residual stale-NDS prose outside captions** (the metric was replaced by auc_norm): paper.tex L462, L464, L493, L495, L551, L556, L571, L573, L598 (abbreviation list), L620 (Availability of data section). Ten body/abbreviation sites plus the caption sites in T3, T5, F8 and Additional files 2, 3, 4, 6, 7, 8, 10, 11.

**Note 8 — figure filenames vs figure numbers.** `fig1_global_overview.png` renders as Figure **4**; `fig3_ranking_consistency.png` renders as Additional file **7**; `fig2_anova_decomposition.png` is the only one whose filename matches its number. Cosmetic, but it makes every cross-check between manuscript and `results/paper_figures_v2/` error-prone.

---

## E. AGAINST THE JOURNAL'S OWN STATED RULES

**There is no numeric cap on figures or tables anywhere in either guideline file.** `JCHEMINF_GUIDELINES_VERBATIM.md` L252–254 delegates entirely: *"### Figures, tables and additional files / See General formatting guidelines for information on how to format figures, tables and additional files."* — and `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` states no count limit either. 8 figures + 6 tables is unremarkable for a JCheminf benchmarking paper; **the problem is duplication, not count** (F2≡T3, F4b≡T4, F5≈T5 — collapsing those three pairs would take the paper to 6 figures + 4 tables with zero information loss).

Rules the manuscript currently breaks, quoted verbatim with line numbers:

1. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L172**: *"Results that would otherwise be indicated as 'data not shown' should be included as additional files."* → violated by `paper.tex` **L489**: *"Heteroscedastic results (not shown) are qualitatively consistent."*
2. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L154**: *"Tables should be numbered and cited in the text in sequence using Arabic numerals (i.e. Table 1, Table 2 etc.)."* → **Table 1 (`tab:metrics_summary`, paper.tex L266) is never cited**, and Table 2 (`tab:regression_noise`) is first cited at L222, before Table 1 appears.
3. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L172**: *"Each additional file should be cited in sequence within the main body of text."* → violated: first-citation order in `paper.tex` is 1 (L197), **12 (L197)**, 1 (L208), 5 (L247), 2–4 (L264), 5 (L422), 6 & 7 (L433), 8 (L466), 9 (L531), 10 (L551), 11 (L560). Additional file 12 is cited tenth-from-last-but-first.
4. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L102**: *"Figure titles (max 15 words) and legends (max 300 words) should be provided in the main manuscript, not in the graphic file."* → no figure in `paper.tex` has a separable ≤15-word title; each is a single caption blob (e.g. L418, L427, L556). All are well under the 300-word legend cap. Related: **L103**: *"Figure keys should be incorporated into the graphic, not into the legend of the figure"* — F7's PDV restriction currently lives in the graphic's suptitle and nowhere in the caption, which is the inverse of what's needed (the restriction is a caption obligation, not a key).
5. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L159**: *"Table titles (max 15 words) should be included above the table, and legends (max 300 words) should be included underneath the table."* → `paper.tex` L305–308, L344–348 place the caption *below* the table via `\caption` after `\end{tabular}`; L387, L437, L470, L503 place it above. Inconsistent, and neither form separates a ≤15-word title.
6. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L162**: *"Commas should not be used to indicate numerical values."* → table cells are clean; but check at copy-edit, since `paper.tex` uses `N = 10{,}000` (L193, L556) and `N = 1,482` (L197) in prose and in the F8 caption L556.
7. `JCHEMINF_GUIDELINES_VERBATIM.md` **L14**: *"Research articles should include an abstract that does not exceed 350 words."* → compliant: `paper.tex` L164 = 229 words, L167 Scientific Contribution = 62 words, 291 total. **L19**: *"use a maximum of 3 sentences"* for the Scientific Contribution → L167 is 2 sentences ✓.
8. `JCHEMINF_GUIDELINES_VERBATIM.md` **L16**: *"A graphical abstract can be supplied…"* — optional; `paper.tex` supplies none.
9. `JCHEMINF_GENERAL_FORMATTING_VERBATIM.md` **L161**: *"Color and shading may not be used. Parts of the table can be highlighted using superscript, numbering, lettering, symbols or bold text, the meaning of which should be explained in a table legend."* → the manuscript's tables use bold only and explain it in the legends (L387, L437, L470, L503) ✓.

---

## F. TOP-LINE VERDICT SUMMARY

- **Cut or merge outright (zero information loss):** F2 (≡T3), F4 panel b (≡T4), F5 (≈T5). Main text drops from 8 figures + 6 tables to 6 + 4.
- **Replace:** F8 — its averaging over strategies × representations × replicates is not repairable by editing the caption.
- **Fix before anything else (data wrong, not just wording):** T3 Robustness half (Note 1), T5 significance flip (Note 2), Additional file 5 (Note 3), `additional_files.tex` L24 graphicspath (Note 4).
- **Metric relabel NDS→AUC_norm:** T3 (L387), T5 (L470, L475), F8 (L556); body L462, L464, L493, L495, L551, L571, L573, L598, L620; Additional files 2, 3, 4, 6, 7, 8, 10, 11.
- **Unsourced numbers to regenerate or delete:** Kendall's W (L433, L573), ρ=0.82 (L413), 91%/72% (L493), "<11%, p>0.25" (L464).
- **Missing justification to write:** why PDV and why Gaussian, for F3–F7. The supporting evidence exists (`table1_anova_summary.csv`, Robust_Rep_η² 0.22–7.89%) and is currently only a source-code comment at `scripts/generate_paper_figures_v2.py` L1502–1505.