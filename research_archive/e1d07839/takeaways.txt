[
 "Verified everything locally with pandas against `/Users/apunt/repos/KIRBy/tests/results/validation/{logd,caco2,herg}/all_results.csv` (16,170\u00d710 each) and `/Users/apunt/repos/qsar_qm_models/results/paper_figures_v2/*.csv`.\n\n---\n\n## A1 \u2014 SURVIVES-WITH-CAVEAT\n\n**Counts reproduce exactly.** \u03c3=0, GP dropped: 1,440 rows/dataset (1,470 incl. GP). Neural R\u00b2<0 per 180-run cell \u2014 LogD MHG-GNN 90, ECFP4 1, PDV 0, SNS 0; Caco-2 131/2/0/0; hERG MHG-GNN 57, PDV 82, ECFP4 6, SNS 1. **Non-neural 0/180 in all 12 cells (0/2,160).** QM9 half verified: `excluded_configs.csv` = 48 rows, 4 Bayesian/variational models \u00d7 {mhggnn, mol2vec} \u00d7 6 strategies, baseline \u22120.190\u2026+0.072; `filtered_catastrophic_iterations.csv` 245 rows, 34 at \u03c3=0, all `dnn_bnn_full`/`dnn_vbll`/`mlp_vbll` on mhggnn/mol2vec. \u03c3=0 noise-off confirmed in code: `scripts/process_and_train.py:1609` `'noise': s > 0`; `rust/src/main.rs:750` `if config.noise && apply_noise`.\n\n**Caveats (both fatal to the sentence as written):**\n1. **\"no kernel model ever failed\" is FALSE.** GP on hERG\u00d7PDV: 18/30 clean runs R\u00b2<0, median R\u00b2 = **\u22120.288** (LogD 0.789, Caco-2 0.484). GP was silently dropped from the count. The statement must read \"no tree ensemble or SVM\", with GP's hERG failure stated.\n2. **The strategy-SD figure does not reproduce.** Claimed medians 0.0003/0.0015/0.0028; actual median across-strategy SD within (model,rep,fold) = **0.00345 / 0.0055 / 0.0068**. Off by 2\u201310\u00d7.\n3. Minor: quoted hERG-PDV medians mis-assign NGBoost/SVM (actual NGBoost 0.463, SVM 0.445; quoted 0.446/0.463).\n4. Non-\u03c3=0 scope: `filtered_catastrophic_iterations.csv` does contain one `xgboost` row, so \"no tree ever fails\" is a \u03c3=0-only statement.\n\nFloat is buildable today.\n\n---\n\n## A2 \u2014 DIES\n\nThe stated ranges reproduce to the decimal (LogD M 27.3\u201343.8 / I 26.4\u201335.6 / R 17.8\u201326.2; Caco-2 M 23.0\u201354.0 / I 13.8\u201326.5 / R 7.0\u201321.0; hERG M 45.5\u201355.3 / I 9.2\u201318.7 / R 1.1\u201311.4). **The ordering claim built on them is false.** Re-running the same decomposition: Model is *not* the largest term in **16 of the 72** cells.\n\n- LogD, 6 failures, Interaction wins: `legacy \u03c3=0.0` M 28.5 < I 32.8; `threshold \u03c3=0.6` M 32.1 < I 35.6; `hetero \u03c3=1.0` M 27.3 < I 33.9; also hetero 0.6, legacy 0.6, valprop 0.3.\n- Caco-2, 10 failures, **Residual** wins: `legacy \u03c3=0.3` M 23.0 < Res 36.7; `quantile \u03c3=0.3` M 26.0 < Res 41.1; `valprop \u03c3=0.3` M 25.0 < Res 39.7; etc.\n- hERG is the only dataset where Model wins in all 24.\n\nSo \"at every noise level and under every one of the six noise strategies\" is refuted, and the Gaussian/\u03c3=0 LogD cell \u2014 the most quotable one \u2014 is an interaction-dominant cell, i.e. it *agrees* with QM9 rather than opposing it. Second, independent kill: the whole decomposition rests on an unstated `r2.clip(lower=0)`; unfloored, all four terms invert (see A3). Third, the QM9 half is `evidence_status: CONTAMINATED` by the drafter's own admission.\n\n---\n\n## A3 \u2014 DIES as a quantitative claim, SURVIVES as the A1 contingency table\n\nEvery reported \u03b7\u00b2 reproduces exactly (non-neural LogD 34.8/26.2/11.3/27.7, Caco-2 9.6/2.3/10.9/77.2, hERG 5.3/4.2/6.7/83.8; neural 16.8/53.5/5.8/23.9, 18.7/49.2/5.4/26.8, 27.9/31.8/9.1/31.2; pooled 32.1/20.7/27.3/20.0, 31.2/18.9/21.3/28.6, 52.2/8.9/14.0/24.9). **But they are an artifact of the R\u00b2-floor-at-0 step.** Same code, flooring removed:\n\n| neural-half \u03b7\u00b2 (M/R/I/Res) | floored | unfloored |\n|---|---|---|\n| LogD | 16.8 / **53.5** / 5.8 / 23.9 | 3.0 / 8.1 / 9.3 / **79.6** |\n| Caco-2 | 18.7 / **49.2** / 5.4 / 26.8 | 3.0 / 12.2 / 8.5 / **76.3** |\n| hERG | 27.9 / **31.8** / 9.1 / 31.2 | 0.4 / 6.3 / 1.1 / **92.2** |\n\n\"Representation is the single largest term among neural models\" holds only under a clipping choice that converts every catastrophic run into a tie at zero. Unfloored, representation is 6\u201312% and residual is 76\u201392%. Additional problems: (a) hERG's floored margin is 31.8 vs 31.2 residual \u2014 a rounding-width lead; (b) LogD non-neural rep = 26.2%, above the stated \"at most a quarter\"; (c) the clean neural/non-neural partition again requires dropping GP, which by the stated rule (median clean R\u00b2 < 0.3 anywhere) belongs in the \"ever fails\" set. The slopegraph float is buildable and worth building; the \u03b7\u00b2 insets are not defensible without reporting both flooring choices.\n\n---\n\n## A4 \u2014 SURVIVES-WITH-CAVEAT\n\nEvery number reproduces: worst clean run LogD \u2212100.43, Caco-2 \u2212145.55, hERG \u22123054.99; max within-(model,rep,fold) SD 39.94 / 54.34 / 1159.97; hERG DNN\u00d7PDV median 0.183; neural-on-PDV 82/180 negative; QM9 catastrophic-row composition as stated; `CATASTROPHIC_R2_THRESHOLD = -0.5` at `generate_paper_figures_v2.py:437`.\n\n**Caveat 1 \u2014 the statement names the wrong axis.** The cited SDs are *within* a fixed (model, rep, **fold**) cell, i.e. run-to-run at constant fold. That proves something stronger than \"one fold vs another\"; say so.\n\n**Caveat 2 \u2014 the \"30 replicate runs\" is not 30 for half the roster.** Fraction of (model,rep,fold) cells with *exactly zero* across-strategy SD at \u03c3=0: LightGBM 1.00, QRF 1.00, SVM 1.00, XGBoost 1.00 on all three datasets; RF 0.80/0.70/0.05; every neural model 0.00. The deterministic models' six \"strategy runs\" at \u03c3=0 are byte-identical duplicates of one run, so n=5, not 30. Point estimates are unaffected (balanced duplication), but any SE, df, or p-value computed on the pooled \u03c3=0 set is inflated 6\u00d7, and the strip-plot float will draw six coincident markers per fold for those models. Annotate it.\n\n---\n\n## A5 \u2014 SURVIVES\n\nDegeneracy fully confirmed. `table_validation_anova.csv`: 91.8/4.2/4.0/**0.0** (LogD), 92.4/0.9/6.7/**0.0** (Caco-2), 95.2/0.2/4.6/**0.0** (hERG) \u2014 each row sums to exactly 100.0. Input `table_validation_auc_full.csv` filtered to `strategy=='legacy'` with `ANOVA_REPS_EXCLUDE` (`:1593`, `:1596`): **max cell size = 1** for every (model, rep) group; n = 19 (LogD), 19 (Caco-2 \u2014 the CSV records 18), 18 (hERG) against 7\u00d73 = 21 possible cells, so the two-way-with-interaction model is rank-deficient and residual df \u2264 0. Roster is `dnn, lgb, ngboost, qrf, rf, svm, xgboost` \u2014 no Bayesian/VBLL/MLP variant, i.e. the entire population that carries the interaction is absent. Residual computed at `:1430`, written at `:1648`, not plotted at `:1642-1643`.\n\n**Two corrections to the write-up, one strengthening:**\n- The KIRBy comparison numbers are **means labelled as medians**. Current-tree DNN\u00d7MHG-GNN LogD: mean 0.204, **median 0.491**; DNN\u00d7ECFP4: mean 0.702, median 0.708. Published values are 0.866 and 0.808.\n- Stronger evidence for the \"older tree\" claim, verified: `/Users/apunt/repos/KIRBy/tests/results_server/alternative_full/logd/all_results.csv` (8-model roster, Mar) gives DNN\u00d7PDV \u03c3=0 median **\u22120.011** vs the current tree's **+0.796**, and DNN\u00d7MHG-GNN **0.791** vs **0.491**. The two trees disagree on *which cells are learnable*, not merely on magnitude \u2014 which is exactly why DNN\u00d7continuous_pdv is missing from the LogD ANOVA input (killed by `VALIDATION_BASELINE_THRESHOLD = 0.3`).\n- Retention-mirage check: `auc_norm` is normalised by the clean baseline and gated at 0.3, so a model clearing 0.3 with a weak baseline still gets a full-scale AUC. The replacement float should carry `baseline_r2` alongside.\n\nReplacement float is buildable from `KIRBy/tests/results/validation/*` today, folds intact. No ARC re-run required.\n\n---\n\n## Missed takeaways the data supports\n\n**M1 \u2014 On hERG, the descriptor vector is the representation that breaks neural networks, not the learned embedding.** \u03c3=0 median R\u00b2 for all six neural models on hERG\u00d7PDV: BNN-Full \u22120.141, MLP-VBLL-Full \u22120.066, MLP-BNN-Full \u22120.034, VBLL-Full 0.095, MLP 0.126, DNN 0.183 \u2014 every one below 0.3, and 82/180 runs negative, *worse* than MHG-GNN's 57/180. On LogD and Caco-2 the pattern is the exact inverse (PDV 0/180, MHG-GNN 90 and 131). This directly refutes paper.tex L462 (\"PDV stood out as having particularly strong robustness\"), L493 (\"PDV configurations produced the shallowest mean NDSs\"), and the \"embeddings are the problem\" framing. It is also the cleanest single piece of evidence for the pair-not-the-part thesis A1 is trying to make.\n\n**M2 \u2014 Deterministic models have zero run-to-run variance by construction; the \"replicate\" axis is fictitious for half the roster.** LightGBM/QRF/SVM/XGBoost: 100% of (model,rep,fold) cells have zero across-strategy SD at \u03c3=0, all three datasets; RF 80/70/5%; all neural models 0%. Consequence: the residual term in every non-neural decomposition (Caco-2 77.2%, hERG 83.8%) is pure *fold* variance, and every n reported on pooled \u03c3=0 data is 6\u00d7 the true independent count. Needs stating in Methods before any of A1\u2013A4 is published.\n\n**M3 \u2014 The variance decomposition is not identified until the R\u00b2-transform is fixed.** Flooring at 0 versus not flips every \u03b7\u00b2 in the paper's framework (neural rep 53.5\u21928.1 on LogD; residual 23.9\u219279.6). No \u03b7\u00b2 number \u2014 QM9's or the validation set's \u2014 is interpretable without the transform stated in the caption. This is a prerequisite for T3 `tab:anova_decomposition` and Additional file 10, independent of the noise bug and the re-run.",
 "## K1 \u2014 **DIES**\n\nSecond clause is false. Recomputed clean-R\u00b2 two-way \u03b7\u00b2 per dataset\u00d7strategy from `/Users/apunt/repos/KIRBy/tests/results/validation/{caco2,herg,logd}/all_results.csv` (48,510 rows; 3,340/3,564 cells kept \u2014 drafter's gate/filter counts reproduce exactly): **representation \u03b7\u00b2 never exceeds model \u03b7\u00b2 on clean R\u00b2, in 0 of 18 cells.** By dataset, rep \u03b7\u00b2 on clean R\u00b2 = hERG 0.8\u20132.9, Caco-2 2.8\u20138.0, LogD 17.5\u201323.7; model \u03b7\u00b2 = 14.9\u201331.9, 10.8\u201318.9, 46.4\u201352.3. Representation does not \"decide how accurate you were to begin with\" anywhere; it is the *smallest* structured term on two of three datasets. The drafter's \"cleanest triple, LogD\" is the only dataset where rep is even second, and it is presented as representative.\n\nFirst clause is also only half-true: on retention, model \u03b7\u00b2 > rep \u03b7\u00b2 in 18/18, but model \u03b7\u00b2 > **interaction** \u03b7\u00b2 in only **10/18** (LogD: interaction 27.9\u201356.0 vs model 9.0\u201351.7; LogD/hetero model 9.0, rep 7.0, interaction 56.0 \u2014 model and rep are a tie there). The pairing, not the model, sets the decline rate on LogD.\n\nWhat survives is only the *relative* asymmetry: rep's share of accuracy variance exceeds its share of retention variance (rep on retention 1.1\u201310.6 in all 18). Minor: drafter's quoted ranges are 0.5\u20131.0 pt off mine (rep@0.6 max 23.2 not 24.0; clean max 23.7 not 24.5; LogD R\u00b2@0.6 min 16.6 not 20.6). QM9 half additionally rests on `table1_anova_summary.csv` performance \u03b7\u00b2 measured at \u03c3=0.3 \u2014 contaminated.\n\n## K2 \u2014 **SURVIVES-WITH-CAVEAT**\n\nEvery number verified. `deep_qm9_model_robustness_by_strategy.csv` mean retention per strategy = outlier 0.944, hetero 0.908, quantile 0.846, legacy 0.825, valprop 0.654, threshold 0.587; `table1_anova_summary.csv` Robust_Model_\u03b7\u00b2 = 10.29, 13.99, 36.79, 43.77, 52.54, 54.68 in that same order \u2014 a perfect rank inversion, \u03c1 = \u22121.00. External replication reproduces to three decimals: hERG \u22120.886 (p=0.019), Caco-2 \u22120.886 (p=0.019), LogD \u22120.943 (p=0.005). \u03c1(retention, residual \u03b7\u00b2) = +0.714 / +0.943 / +0.829.\n\nCaveats: (a) both axes are functions of the same `auc_norm`, so this is partly definitional, not an empirical discovery \u2014 say so; (b) the QM9 retention means average over rep and iteration; (c) the \"measurement floor\" reading is confirmed \u2014 hetero costs 9.2% of baseline on QM9 and 2.3% on LogD.\n\n## K3 \u2014 **SURVIVES-WITH-CAVEAT**\n\nDirection reproduces; three numbers are off. Best-by-retention and best-by-R\u00b2@0.6 agree in **18 of 72** cells (drafter: 21), LogD **0/24** \u2713, Caco-2 14/24, hERG 4/24. LightGBM tops R\u00b2@0.6 in 21 cells and retention in **0** \u2713. NGBoost tops retention in **24** (drafter: 25) and accuracy in **11** (drafter: 12). Median \u03c1(retention, clean R\u00b2) across models = **\u22120.105** (drafter: \u22120.09). LogD worked example exact: NGBoost retention 0.982 rank 1/13, R\u00b2@0.6 0.618 rank 9/13, clean 0.627 vs LightGBM 0.752.\n\nCaveats: (i) the LogD worked example averages across all six strategies and all reps \u2014 it violates the no-averaging rule and cannot be quoted as stated; (ii) **the float is not buildable as drawn**: the hERG GP case (clean R\u00b2 = \u22120.156, the only sub-zero GP cell) is removed by the baseline \u22650.3 gate the float also declares, so the \"dashed line at R\u00b2=0 makes it visible\" element is self-contradictory \u2014 drop it or drop the gate; (iii) R\u00b2@0.6 does not exist for QM9 in any artifact (`calculate_robustness`, `scripts/generate_paper_figures_v2.py:1803-1846`, emits only auc_norm/baseline_r2/n_sigma) \u2713 confirmed.\n\n## K4 \u2014 **SURVIVES**\n\nEvery figure verified at \u03c3=0, untouched by the noise bug. Neural: 72 cells, 30 below 0.3, 20 below 0, min \u2212197.428 = MLP-VBLL-Full / hERG / PDV \u2713. Trees: 60 cells, 0 below 0.3, min 0.383 \u2713. SVM: 12 cells, 0 below, min 0.364 \u2713. hERG PDV collapse \u221236.1 to \u2212197.4 across all six neural models \u2713; MHG-GNN breaks them on LogD (\u22120.64 to \u221210.3) and Caco-2 (\u22121.55 to \u221220.4) \u2713. `excluded_configs.csv`: 48 rows, exactly {dnn_bnn_full, dnn_vbll, mlp_bnn_full, mlp_vbll} \u00d7 {mhggnn, mol2vec}, baseline \u22120.190 to +0.072 \u2713. `filtered_catastrophic_iterations.csv`: 245 rows; mlp_vbll 137 + dnn_vbll 95 = 232 \u2713; mol2vec 146 + mhggnn 97 = 243 (drafter conflated the model-count 232 with the rep split \u2014 reword).\n\nFilter-dependence claim confirmed by direct rerun: with the divergent-fold filter, retention \u03b7\u00b2 = model 9.0\u201369.8, rep 1.1\u201310.6, interaction 8.9\u201356.0. Without it: **model 6.3\u201364.4, rep 2.6\u201315.0, interaction 10.0\u201380.6** (drafter said 6.2\u201363.9 / 9.6\u201381.0 \u2014 matches). Interaction dominates 18/18 cells unfiltered.\n\nOne correction for the float: GP on hERG/PDV is \u22120.156, so \"no kernel model ever fails\" is wrong if the family header says \"kernel\". SVM never fails; GP does.\n\n## K5 \u2014 **SURVIVES-WITH-CAVEAT** (the comparative half dies)\n\nRecomputed: strategy-pair rank agreement n=180, **median 0.702**, IQR 0.51\u20130.84, min \u22120.287. Rep-pair agreement n=108, **median 0.704**, IQR 0.49\u20130.86, min \u22120.238. The two distributions are indistinguishable \u2014 so \"strategy transfer is no better than representation transfer\" **survives**, but \"representation transfer is, if anything, the more reliable of the two\" (drafter: 0.66 vs 0.77) **does not reproduce**. Drop that sentence; it is the only thing the strip panel in the proposed float exists to show.\n\nThe challenge to paper L433 (`W = 0.9121` \u2192 \"largely strategy-independent\") stands: `table6_kendalls_w.txt` collapses across all reps (`generate_paper_figures_v2.py:3711`), so it is not comparable to a per-representation coefficient.\n\nDose-artefact caveat is correct and must be kept: `rust/src/main.rs:364-373` compares the **raw** label to \u00b11.0 (`:1195-1199`), and `--strategy_params` is never passed (`scripts/process_and_train.py:1628-1641`), so cut-points are inert on QM9 and threshold = Gaussian at 2\u03c3.\n\n---\n\n## MISSED \u2014 supported, and stronger than K5\n\n**Strategy \"severity\" is not a property of the noise pattern; it is a property of the label's raw scale, and it re-orders across datasets.** Mean retention per strategy (folds/models/reps within dataset):\n\n| | hetero | legacy | outlier | quantile | threshold | valprop |\n|---|---|---|---|---|---|---|\n| QM9 | 0.908 | 0.825 | 0.944 | 0.846 | 0.587 | 0.654 |\n| LogD | 0.977 | 0.928 | 0.940 | 0.935 | 0.821 | 0.892 |\n| Caco-2 | 0.915 | 0.582 | 0.625 | 0.599 | **0.707** | 0.555 |\n| hERG | 0.926 | 0.828 | 0.873 | 0.822 | 0.540 | 0.535 |\n\nThreshold is the harshest strategy on QM9 and LogD (rank 6/6), fifth on hERG, and the **second-mildest on Caco-2** (rank 2/6). Outlier is mildest on QM9 but third on Caco-2. Mechanism is in the code, not the data: threshold's cut-points (`main.rs:364-373`, \u00b11.0 raw), quantile's percentiles (`:342-348`), outlier's z-gate (`:387-396`) and hetero's `\u03c3\u00b7sqrt(0.1+0.05|v|)` (`:413`) are all evaluated on the **raw** target, so one unit of \u03c3 delivers a different realised dose per endpoint. Combined with K2 (\u03b7\u00b2 tracks dose), this means *every* per-strategy \u03b7\u00b2 comparison in the paper is a comparison of doses across incommensurable scales. This subsumes K5's caveat and is the honest answer to Q3: the paper cannot claim strategies generalise or fail to generalise until \u03c3 is normalised to a realised noise SD.\n\nFloat: same 4\u00d76 mean-retention matrix above plus, beside it, the analytically derived RMS dose per strategy at \u03c3=1 computed on each dataset's own label distribution from the Rust formulas. Buildable today from `all_results.csv` + the label files; no re-run needed.",
 "## Verification performed\nAll numbers below computed by me from the named files; nothing taken from markdown assertions.\n\n---\n\n**NM1 \u2014 SURVIVES (with one factual correction to the float spec)**\n\nReproduced exactly on `/Users/apunt/repos/qsar_qm_models/data/QM9/raw/gdb9.sdf.csv` (133,885 rows, `gap`\u00d727.2114): CV of \u03c3_i = 0.000 legacy / 0.003 threshold / 0.074 hetero / 0.077 valprop / 1.582 quantile / 2.629 outlier; top-10%-of-molecules share of injected variance 10.0/10.0/12.4/12.6 vs 49.4/96.3. Min gap 0.6694 eV, next 1.0231, exactly 1 molecule < 1.0 eV. Formulas verified at `rust/src/main.rs:309-317` (valprop), `:322-352` (quantile), `:355-376` (threshold), `:379-404` (outlier), `:406-415` (hetero), defaults `:1169-1217`.\n\nCorrection the drafter got wrong: the runs use `qm9[:args.sample_size]` \u2014 the **first 10,000** molecules (`scripts/process_and_train.py:625, 640, 667`), i.e. the smallest ones. On the actual first-8,000 training set: min gap = **2.084 eV, zero** molecules below 1.0 eV, so threshold CV = **0.000 exactly** \u2014 threshold *is* Gaussian at 2\u03c3 with no exception at all, and the doses are 1.000/2.000/1.701/0.899/0.502/0.669 (these, not the full-file 1.688/0.490/0.665 in the float caption, are the run-relevant numbers and they match the MEMORY table). Not touched by the held-out-noise bug (analytic). Float buildable today.\n\n---\n\n**NM2 \u2014 SURVIVES-WITH-CAVEAT**\n\nAll quoted correlations reproduce. `results/paper_figures_v2/table2_auc_by_strategy_pdv.csv` column means: Threshold 0.603, Value-Prop. 0.665, Gaussian 0.830, Quantile 0.850, Hetero 0.911, Outlier 0.945 against doses 2.000/1.701/1.000/0.899/0.669/0.502 \u2192 Spearman **\u22121.000**. Across-model range: Outlier 0.023, Threshold 0.129 (matches paper L460's \"0.02\"/\"0.13\"), Spearman vs dose **+1.000**. `table_validation_auc_full.csv`: LogD **\u22120.943**, Caco-2 **\u22121.000**, hERG **\u22120.943**; pooled 24 cells in label-SD units **\u22120.861** (drafter said \u22120.851), p<1e-4.\n\nTwo checks it passes that the drafter didn't run: (i) the QM9 ordering is monotone for **all 11 models individually**, not just the MEAN column \u2014 survives disaggregation; (ii) it is not a retention mirage \u2014 `baseline_r2` is invariant across strategies within a (dataset, model, rep) cell (mean range 0.0069, >0.01 in only 9 of 78 cells), so auc_norm differences between strategies cannot come from a moved denominator.\n\nCaveats: (a) the QM9 half is bug-contaminated (`rust/src/main.rs:740-748`; fix uncommitted at `:623`, `:750`, `:1028/:1055/:1082`, no release binary) \u2014 quote only the external half until re-run; (b) n=6 per dataset and the four datasets share nearly the same dose ordering, so they are four correlated replications, not four independent confirmations \u2014 do not report the pooled p as if n=24; (c) \"**every** model\" is false on the externals \u2014 LogD and hERG each invert quantile/Gaussian at the aggregate level.\n\n---\n\n**NM3 \u2014 DIES as stated**\n\nI reproduced the analysis independently (48,510 rows, `/Users/apunt/repos/KIRBy/tests/results/validation/{logd,caco2,herg}/all_results.csv`; clean-R\u00b2\u22650.3 gate; strategy R\u00b2 at \u03c3 vs Gaussian curve interpolated at \u03c3\u00d7dose-ratio; 5,324 comparisons). Signed mean gap at matched dose: hetero **\u22120.002**, threshold \u22120.007, quantile **+0.006**, outlier **\u22120.027**, valprop **\u22120.052**. Mean |gap| 0.124\u21920.021 (hetero), 0.094\u21920.048 (threshold), 0.060\u21920.059 (quantile), 0.080\u21920.061 (outlier), 0.094\u21920.071 (valprop). The drafter's numbers replicate.\n\nThe numbers refute the statement's mechanism clause:\n- **valprop**, the *most homoscedastic* strategy on these endpoints (CV 0.036\u20130.049), carries **twice** outlier's residual (\u22120.052 vs \u22120.027). \"Concentration is the only mechanism that adds anything\" is false on its own data.\n- **quantile**, the second-most concentrated strategy (CV 1.44\u20131.57), has a residual of **+0.006** \u2014 zero. Concentration does not predict residual damage.\n- outlier's residual is **one dataset**: Caco-2 \u22120.061, hERG \u22120.021, LogD **\u22120.002**. It is not a general mechanism.\n\nSalvageable core: \"at matched dose, heteroscedastic and threshold noise collapse onto the Gaussian curve (|gap| falls 83% and 49%), and quantile was never off it \u2014 dose, not distribution, is what the \u03c3 knob buys.\" Outlier and valprop residuals both need per-dataset reporting and valprop's needs the truncation-window fix before it is quotable (hERG dose ratio 1.286 truncates 3 of 11 \u03c3 points). Also: 13 models \u00d7 4 reps is wrong \u2014 GP exists only for PDV (3,960\u00d73 + 4,290 = 16,170 per dataset).\n\n---\n\n**NM4 \u2014 SURVIVES (stronger than stated; its three quoted auc_norm values do not reproduce)**\n\nAnalytic half confirmed exactly. Threshold % of molecules getting 2\u03c3: QM9 100.0% (first-8000; 99.99925% on the full file), LogD **85.4%** (n=5,039), log10 Caco-2 efflux **9.2%** (n=2,161), hERG **100%** (n=1,415). Threshold CV of \u03c3_i: 0.000 / 0.390 / **1.998** / 0.000 \u2014 from perfectly homoscedastic to more concentrated than quantile. Raw-unit comparison verified at `rust/src/main.rs:364-373` with `target_values` raw at `:542` and normalisation only later at `:759-761`; same defaults in `NoiseInject/noiseInject/core.py:96-115`.\n\nIts results-side numbers are wrong: from `table_validation_auc_full.csv` threshold on hERG is **0.396** (not 0.439) and on Caco-2 **0.662** vs Gaussian **0.404** (not 0.674 / 0.398); no rep-subset, balanced subset, or median reproduces the quoted trio. The qualitative reversal is real and holds in **all four reps** individually (threshold lowest of six on hERG; threshold above Gaussian on Caco-2 in every rep) \u2014 so it survives disaggregation, but replace the numbers.\n\nUpgrade: NM4 cites L383. The harder target is **paper.tex L354**, which states threshold uses \"$|y| > 1.0$ **(on normalized data)**\". The code compares the raw label. That is a flat methods misstatement, not an interpretation.\n\n---\n\n**NM5 \u2014 SURVIVES**\n\n`rust/src/main.rs:1177`: `proportionality_factor \u2026 unwrap_or(0.1)`. `NoiseInject/noiseInject/core.py:157`: `proportionality_factor: float = 0.05`. `KIRBy/tests/alternative_data_noise_robustness.py:756/791` construct `NoiseInjectorRegression(strategy=\u2026, random_state=42)` and call `injector.inject(y_train, sigma)` at `:760/:795` with no `strategy_params` \u2192 0.05 in force. `--strategy_params` never passed from `scripts/process_and_train.py:1628-1641` \u2192 0.1 in force on QM9. Other five match (quantile 0.9/0.1 \u00d72/2/0.1; threshold \u00b11.0 \u00d72/2/0.1; outlier z>2 \u00d73/0.1 with population SD in both; hetero \u03b1=0.1\u03c3\u00b2, \u03b2=0.05\u03c3\u00b2; legacy plain). Dose impact on the first-8000 QM9 train set: **1.701\u03c3 (0.1) vs 1.350\u03c3 (0.05)**.\n\nOne nit: quantile is not bit-identical either \u2014 Rust indexes `sorted_values[(n*0.9) as usize]` (`:325-334`), NumPy `np.quantile` interpolates. Immaterial at n\u22651,400; mention it in the spec table or the \"Identical? Yes\" column is not strictly true.\n\n---\n\n## Missed, and the data supports them\n\n**NM6 \u2014 The paper's central methods promise about \u03c3 is false, in two places.** `paper.tex:222`: \"each strategy uses \u03c3 to control the magnitude of injected noise \u2026 regardless of how the noise is distributed across samples.\" `paper.tex:354`: \"while the relative noise distribution differs between strategies, the difficulty scaling controlled by \u03c3 is consistent.\" At \u03c3=1 the realised RMS dose spans **0.502 \u2192 2.000 eV, a 4.0\u00d7 range** on the QM9 training set (and 0.343 \u2192 1.848 on Caco-2, 0.621 \u2192 2.000 on hERG). These two sentences are the load-bearing justification for reading every cross-strategy comparison in the paper as a mechanism comparison, and they are contradicted by the injector. This is more directly citable than L383 and no takeaway names it. SOLID, analytic, bug-immune.\n\n**NM7 \u2014 Figure 1's caption describes data the figure does not contain, and two of its six formulas are not the ones that were run.** `paper.tex:359` says the panels show \"the QM9 HOMO\u2013LUMO gap label distribution\". `scripts/generate_paper_figures_v2.py:2538-2546` generates a synthetic 3-component Gaussian mixture (n=2000, means \u22120.5/0.2/0.8) \u2014 no QM9 label ever enters the figure. Worse, the Python re-implementation diverges from the Rust injector on two strategies: valprop is **additive** there (`\u03c3 + 0.1|y|`, `:2557`) but multiplicative in Rust (`\u03c3(1+0.1|y|)`, `:313`); threshold uses a **median split** (`:2563-2565`) but an absolute \u00b11.0 cut in Rust (`:364-373`). The figure a reader uses to understand the six strategies shows a different dataset and two different rules. Fix is the NM1 float; the caption must be rewritten regardless.",
 "All 27 files exist and I recomputed every number independently. Results:\n\n---\n\n## U1 \u2014 SURVIVES-WITH-CAVEAT\n\n**Verified.** 27 files present at `/Users/apunt/repos/KIRBy/tests/results_server/validation_rerun/{ecfp4,mhg_gnn_pretrained,pdv,sns}_{caco2,herg,logd}/*/{QRF,NGBoost,GP}_*_uncertainty_values.csv` (GP only under `pdv_*`). Schema is 5-col (`sigma,sample_idx,y_true,y_pred,uncertainty`), 11 \u03c3 \u00d7 one fold (n=1007 logd / 432 caco2 / 283 herg). `y_true` bit-identical across all 11 \u03c3 in **27/27** \u2014 confirmed. Noise into `y_train` only: `alternative_data_noise_robustness.py:760` (`y_noisy = y_train if sigma==0.0 else injector.inject(y_train, sigma)`), test never touched; Gaussian-only gate at `:1030`. **Not QM9-contaminated.**\n\nMy recomputation: unc ratio median **2.129** (1.724\u20133.988); err ratio median **1.243** (0.996\u20132.082); unc>err **27/27**; Wilcoxon p=1.49e-8. Exact match to the draft.\n\n**Caveats:**\n1. \"rises monotonically in 27/27\" is **wrong as stated** \u2014 Spearman(\u03c3, mean unc) = 1.0 in **24/27**; QRF\u00d7hERG\u00d7{PDV,SNS,MHGGNN} give 0.9909.\n2. **U1 is the weaker restatement of U3 and should be folded into it.** A ratio-of-ratios has two different floors: mean |error| at \u03c3=0 is bounded below by irreducible model bias, mean uncertainty is not. The clean version of the same fact is the direct quantity: median mean_unc / mean_|err| rises **1.12 \u2192 2.01** monotonically over \u03c3 (range at \u03c3=0: 0.60\u20131.43; at \u03c3=1: 1.31\u20133.84). Quote that, not \"twice as fast\".\n3. Single strategy (Gaussian). Nothing is averaged, but the claim is untested on the other five and untestable without a re-run.\n4. Provenance is better than the draft says: I cross-checked GP/PDV/logd/legacy R\u00b2 between `results_server/validation_rerun/pdv_logd` and the canonical `tests/results/validation_rerun/pdv_logd` \u2014 agrees to \u22640.005 at every \u03c3. The superseded tree is numerically equivalent. `tests/results/**` has **0** `*_uncertainty_values.csv` \u2014 confirmed.\n\n---\n\n## U2 \u2014 SURVIVES-WITH-CAVEAT\n\n**Verified exactly.** \u03c1(unc, |err|) within \u03c3: median **0.289** at \u03c3=0 (0.160\u20130.421) \u2192 **0.069** at \u03c3=1 (\u22120.067\u20130.193); declining **27/27**; median Spearman(\u03c3,\u03c1) = **\u22120.818**; Wilcoxon p=1.49e-8. Healthy subset (R\u00b2\u22650.3 at every \u03c3, n=**11**): 0.229 \u2192 0.079, declining 11/11. All reproduce.\n\n**Caveat (must be in the claim, not a footnote): the decline is driven by uncertainty homogenizing, and a referee will otherwise call range restriction.** Coefficient of variation of predicted uncertainty across molecules falls from median **0.398** (\u03c3=0) to **0.156** (\u03c3=1), declining in **27/27**. GP is extreme: CV 0.398\u21920.098, IQR/median **0.325\u21920.025** \u2014 at \u03c3=1 the GP's uncertainty is essentially a constant, so there is nothing left to rank with. State the mechanism or the finding reads as an artifact.\n\nSecondary: mechanism is not mysterious \u2014 with clean test labels, the extra error from training on noisy labels is overfit-to-noise error, which is not a function of x and is therefore not predictable from any input-based uncertainty. That is a legitimate framing, not a defect.\n\n---\n\n## U3 \u2014 SURVIVES-WITH-CAVEAT\n\n**Verified exactly.** 1\u03c3 coverage median **0.635** (0.378\u20130.759) \u2192 **0.884** (0.731\u20130.988), rising **27/27**; 2\u03c3 **0.903 \u2192 0.984**; healthy subset 0.635 \u2192 0.884. Pooling confirmed: `generate_paper_figures_v2.py:3437-3452` computes coverage over the whole mask with no \u03c3 split; `table4_supp_uncertainty_by_strategy_rep.csv` Coverage 1\u03c3 Gaussian values run 0.652\u20130.786, near-nominal, hiding the drift.\n\n**Caveats:**\n1. **Four** configs go negative-R\u00b2 at \u03c3=1, not three: QRF\u00d7caco2 \u00d7 {ECFP4 (\u22120.793), MHGGNNpretrained (\u22120.485), PDV (\u22120.268), SNS (\u22120.910)}. The draft omits MHGGNN.\n2. **\"Starts roughly right\" is a median artifact \u2014 the per-model split is the real finding.** At \u03c3=0: NGBoost 0.587 (**under**-covered vs 0.68), QRF 0.674 (right), GP 0.732 (**over**). At \u03c3=1: GP 0.949, NGBoost 0.891, QRF 0.849. The three families start in three different calibration regimes and converge on over-coverage. Report the split.\n3. Retention-mirage check passes for the healthy subset; the four collapsed QRF/caco2 cells are exactly the trivial \"near-constant prediction\" case and must be excluded or labelled.\n\n---\n\n## U4 \u2014 arithmetic half SURVIVES; the proposed float DIES\n\n**Table match verified line by line** against `results/paper_figures_v2/table4_supp_uncertainty_by_strategy_rep.csv` (column is `Unc-Noise \u03c1 (pooled)`). 13/14 agree to 2 dp; **GP/SNS is the sole mismatch: paper 0.56 vs file 0.5354** \u2014 the table predates the CSV. Confirmed also: `BNN-\u03b2/PDV` 0.38 \u2194 continuous_pdv 0.3759 (the draft omitted this row from its list).\n\nCode verified verbatim: `generate_paper_figures_v2.py:3423-3424` \u2014 \"*kept only for comparison, do NOT report as per-sample*\"; docstring `:1035-1043` \u2014 the \u03c3-ramp / \"Kolmar effect\" staircase explanation. `paper.tex:499` calls it \"the per-sample Spearman correlation between predicted uncertainty and noise magnitude\". The contradiction is real and the arithmetic argument needs no data.\n\n**Two corrections to the draft's within-\u03c3 list:** it gives 8 values for 10 rows and omits the two largest-magnitude ones \u2014 GP/SNS **\u22120.043** and BNN-\u03b2/continuous_pdv **\u22120.0504**. Largest absolute among the ten remains 0.076 (NGBoost/SNS), so the conclusion is unchanged.\n\n**The float dies.** Both the pooled and the within-\u03c3 columns are computed from `injected_noise`, which is (pre-fix) a *scrambled* vector \u2014 test molecule i carries train molecule i's draw (`rust/src/main.rs:740-748`, index restart `:626`) \u2014 and (post-fix) identically zero, since `apply_noise=false` for val/test (`:1055`, `:1082`). A slope chart of contaminated-vs-contaminated is not publishable. Keep the arithmetic point; drop the empirical panel.\n\n**Landmine the draft missed:** the within-\u03c3=0.3 column is populated for **all six** strategies (33\u201342 rows each), and **Outlier's median is 0.105 with max 0.388** \u2014 three orders of magnitude above Gaussian's 0.000074. Do **not** report this as detection. It is bimodal outlier dosing plus a leakage term from `fix_injected_noise` grouping on `{model,rep,sigma,iteration}` only (`:995`), which pools strategies with different normalisation constants and leaves a deterministic linear function of `y_orig` in the \"noise\" residual. Anyone building the 6-panel float will hit this and misread it.\n\n---\n\n## U5 \u2014 SURVIVES-WITH-CAVEAT\n\nEvery code claim verified (line numbers drift 2\u20134 lines):\n- `scripts/utils.py:80` \u2014 `aleatoric = None`, `total = epistemic` for sampling BNNs. \u2705\n- `scripts/utils.py:162` \u2014 `epistemic = None`, `total = aleatoric` for NGBoost/QRF. \u2705\n- `scripts/utils.py:130` \u2014 `aleatoric = np.full_like(epistemic, np.sqrt(likelihood_noise))`, GP constant. \u2705\n- `scripts/utils.py:188` \u2014 VBLL same, constant. \u2705\n- `models/models.py:1757` \u2014 `pred_vars = test_preds.variance.numpy()` on `model(x_test_tensor)`, **not** `likelihood(model(...))`; `y_pred_std=y_pred_std_uncalibrated` is what reaches `save_uncertainty_values` (`:1793-1795`). The GP's `total` including observation noise is computed at `:1775` and discarded. \u2705\n- `decompose_uncertainty_sampling_heteroscedastic` \u2014 **zero callers repo-wide** (definition only, `utils.py:88`). \u2705 The 2-output head is `models.py:2031` (not 2027), discarded at `:2103` (`output = output[:, 0:1]`). \u2705\n\nCSV values verified: NGBoost/continuous_pdv 0.560/0.560/blank; QRF/continuous_pdv 0.562/0.562/blank; BNN-\u03b1 and BNN-\u03b2 Mean Aleatoric blank in all 8 reps; **GP/pdv Mean Uncertainty 0.629 == Mean Epistemic 0.629 while Mean Aleatoric = 1.4446** (2.3\u00d7). GP/sns and GP/morgan likewise unc == epistemic exactly.\n\n**Caveat:** \"for every model in this study\" overstates. `train_heteroscedastic_gp` (`models.py:6835-6837`, `aleatoric_var = noise_model(x_test_t)`) and `train_evidential_kernel` (`:6964`, `beta/(v(alpha-1))`) **do** compute a genuinely per-molecule aleatoric term \u2014 and then save only `total_std` (`:6851`, `:6977`). The claim is true of every model in `MODEL_ORDER` (`generate_paper_figures_v2.py:283-289`) and of every byte on disk; say \"the paper's roster\", not \"this study\", or a reviewer with the repo will find the counterexample.\n\nSecond caveat: BNN-\u03b2 also has **Mean Epistemic blank** in 6 of 8 reps (sns, smiles, ecfp4, pdv, mhggnn, mol2vec) \u2014 those rows have *neither* component, a stronger version of the point than the draft makes.\n\n---\n\n## MISSED\n\n**U6 \u2014 Uncertainty stops discriminating between molecules under noise.** The strongest claim in this dataset and the mechanism behind both U2 and U3. Across-molecule CV of predicted uncertainty falls from median **0.398** (\u03c3=0) to **0.156** (\u03c3=1), **27/27**; IQR/median falls 0.375 \u2192 0.200. Per model at \u03c3=0\u21921: GP CV 0.398\u21920.098 (IQR/median 0.325\u2192**0.025**), NGBoost 0.265\u21920.137, QRF 0.503\u21920.325. So noise inflates uncertainty **uniformly**, not selectively \u2014 which is precisely why the interval over-covers (U3) and why error-ranking collapses (U2). Buildable today from the same 27 files; same 3\u00d73 layout, y = CV or IQR/median of uncertainty. Not QM9-contaminated, not averaged, and it makes U1/U2/U3 one coherent story instead of three.\n\n**U7 \u2014 The three model families are in three different calibration regimes on clean data.** \u03c3=0 1\u03c3 coverage: NGBoost 0.587 (under), QRF 0.674 (nominal), GP 0.732 (over) \u2014 against 0.68. And the uncertainty inflation is family-ordered: GP 2.41\u00d7, NGBoost 2.20\u00d7, QRF 2.02\u00d7, while error inflation is inversely ordered: QRF 1.39\u00d7, GP 1.21\u00d7, NGBoost 1.06\u00d7. QRF is the only family whose *error* moves substantially; NGBoost's error is essentially flat (min err ratio 0.996 at NGBoost/logd/PDV, R\u00b2 0.699\u21920.698) while its uncertainty nearly doubles. That is a per-family statement U1's median hides entirely.\n\n**U8 \u2014 The effect scales with dataset size, which bears on the supervisor's open Fig-8 flag.** Median uncertainty inflation by dataset: caco2 (n_test 432) **3.64\u00d7**, hERG (283) 2.02\u00d7, logd (1007) 1.93\u00d7; and \u03c3=1 1\u03c3 coverage caco2 0.951 vs logd 0.884, \u03c1 at \u03c3=1 caco2 0.014 vs logd 0.119. Both the over-inflation and the ranking collapse are worst on the smallest set. Cheap to state, already supported, and it is the same dataset-size argument flagged in `paper_insertion_dataset_size_effect`."
]