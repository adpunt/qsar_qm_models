{
 "findings": [
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "943-948 (run_neural_experiment oof block); 769-800 (_oof_predict)",
   "problem": "The out-of-fold block runs 5 extra `train_neural_regression` fits *inside* the sigma loop, after the main fit. `train_neural_regression` consumes the GLOBAL torch RNG (weight init + `DataLoader(shuffle=True)`), and nothing snapshots or restores it. So at every sigma > 0 the main model is initialised and shuffled from a different RNG position than in a run without `--oof-folds`. Tree models are immune (RF/QRF/NGBoost all pin `random_state=42` \u2014 verified deterministic under intervening fits), so this hits exactly the four neural uncertainty models: BNN-Full, VBLL-Full, MLP-BNN-Full, MLP-VBLL-Full.",
   "why_it_matters": "Every `--oof-folds 5` job silently produces DIFFERENT test-set predictions, uncertainties, R2/RMSE and auc_norm for the 4 neural models than the existing validation runs did \u2014 at every sigma except 0.0. `merge_results.py` emits `all_results.csv` and `summary.csv` from these jobs; merging or comparing them against the current validation numbers compares two different experiments. The test-split `uncertainty` column (question B) also comes from a model whose training trajectory was perturbed by the cross-fitting machinery that was supposed to be a side-channel.",
   "repro": "Directly on the shipped function (epochs=5, mlp-full-bnn, 120x6 synthetic):\n  torch.manual_seed(0); a0=T(X,y); a1=T(X,y)                       # oof OFF\n  torch.manual_seed(0); b0=T(X,y); [T(X[:96],y[:96]) for _ in range(5)]; b1=T(X,y)   # oof ON\nOutput:\n  first fit identical: True\n  second fit identical (oof off vs on): False  max|diff|= 0.804369543747046\nControl (same harness): 'NGBoost deterministic under intervening fits: True', 'RF deterministic: True'.",
   "fix": "Wrap the oof block so it cannot move the global stream, e.g. in run_neural_experiment:\n    st = torch.get_rng_state(); cst = torch.cuda.get_rng_state_all() if torch.cuda.is_available() else None\n    om, ou = _oof_predict(_fp, X_train, y_noisy, oof_folds)\n    torch.set_rng_state(st);  (and torch.cuda.set_rng_state_all(cst))\nBetter still, seed deterministically at the top of `train_neural_regression` (e.g. `torch.manual_seed(seed)` with a seed derived from model_type+sigma+fold) so neural fits stop depending on call order at all."
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "1257-1262 (checkpoint) vs 1354-1381 (uncertainty writer)",
   "problem": "The per-fold checkpoint added for timeout protection only writes `all_fold_results` -> `all_results_partial.csv`. The new, expensive artifact \u2014 `all_uncertainties`, i.e. every test row and every out-of-fold training row \u2014 is accumulated in memory and written ONLY after all 5 outer folds finish (line 1354 onwards, past the fold loop). The same diff multiplied per-task compute by (1 + oof_folds) = 6x for the 7 uncertainty models (11 sigmas x 5 folds x 6 fits = 330 model fits per task, vs 55 before), while the generated SLURM scripts drop to `--time=36:00:00` on `medium` \u2014 the previous full run of this script used `--time=71:59:00` on `long` (KIRBy/tests/slurm_scripts/archive/run_alternative_datasets.sh:6).",
   "why_it_matters": "A task that hits the wall clock after 4 of 5 folds loses 100% of the uncertainty and out-of-fold data \u2014 the entire purpose of the re-run \u2014 while leaving an `all_results_partial.csv` that makes it look like the job did something. With NGBoost at 500 estimators on ECFP4 this is not hypothetical: I measured ~25 min per fit at n=2580, d=2048, i.e. ~141 h for the 330 fits in one task. Even a 4x faster cluster node puts NGBoost/GP tasks at or past the 36 h limit, and a timeout costs the full re-submit.",
   "repro": "Timing: `NGBRegressor(n_estimators=25, learning_rate=0.01, random_state=42).fit(X, y)` on X = 2580 x 2048 binary -> 76.8 s => ~25.6 min for 500 estimators => 330 fits = 140.9 h.\nTask size: LogD n=5039 -> per-fold train 4031, minus 20% val = 3225 train rows; 11 sigmas x (1 main + 5 oof) x 5 folds = 330 fits.\nCode: the `_by_config` write loop starts at line 1354, outside `for fold_idx, ... in enumerate(gkf.split(...))` which ends at line 1262.",
   "fix": "Move the uncertainty write inside the fold loop (append/merge per fold \u2014 the existing dedupe on (split, strategy, fold) already makes that idempotent), or dump each fold's frames to `<fname>.fold{k}.csv` and concatenate at the end. Separately, re-check `--time` for NGBoost and GP against the 6x fit count before submitting, or drop to `--oof-folds 3`."
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/qsar_qm_models/slurm_scripts_uncertainty_rerun/merge_results.py",
   "line": "48 (pd.concat of all frames), 66 (to_csv), 72 (pd.read_csv of the same file)",
   "problem": "`_read_all` reads every per-task `*_uncertainty_values.csv`, holds them all in a list and does one `pd.concat(frames)`, adds 5 extra object-dtype columns (`_task_model`, `_task_dataset`, `_task_rep`, `_task_strategy`, `_source`), writes `uncertainty.csv`, and then line 72 reads that whole file back in AGAIN to build coverage.csv. The row count of the new output was not sized: writing all six strategies plus the out-of-fold training split multiplies the old per-config volume by roughly 6 x 6.\n\nMeasured row counts (11 sigmas x 5 folds x (test rows + train rows), x 7 models x 4 reps x 6 strategies):\n  logd  232,760 per config -> 39,103,680\n  caco2  99,825 per config -> 16,770,600\n  herg   65,395 per config -> 10,986,360\n  TOTAL 66,860,640 rows",
   "why_it_matters": "That is a ~12.9 GB CSV and ~28 GB of pandas frames before the `_task_*`/`_source` string columns (realistically 45-60 GB), materialised twice. `merge_results.py` will MemoryError on a login node or a normal compute allocation, after the cluster jobs have already run \u2014 so the stitching step, the one thing standing between the run and the analysis, fails at the end.",
   "repro": "python3: measured 192 bytes/row on disk and 419 bytes/row in pandas for a representative 14-column row block; row-count arithmetic above gives 66,860,640 rows -> 12.9 GB csv / 28.0 GB in memory (excluding the 5 columns merge_results adds).",
   "fix": "Do not build one frame. Stream: open the output with `mode='a', header=(first)` and append each task file, and build coverage.csv incrementally from per-file `groupby(...).size()` counts rather than re-reading the merged file. Or write Parquet partitioned by dataset/strategy. Downcast `sigma`/`fold`/`sample_idx` and make `split`/`strategy`/`model`/`rep`/`dataset` categorical."
  },
  {
   "severity": "MEDIUM",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "1149-1155 (GP subsample), 1170 (y_train_used = y_train_gp), 1240 ('sample_idx': i)",
   "problem": "For GP the runner is handed `X_train_gp, y_train_gp` \u2014 a fixed 2000-row subsample when `len(X_train) > GP_MAX_N` \u2014 and `y_train_used` is set to that subsample. The `train_oof` rows therefore write `sample_idx = i` over the 2000-row subsample, whereas every other model writes `sample_idx` over the full training array. Same `dataset`, same `fold`, same `split`, different molecule identity. `train_noise_scale` / `train_noise_pattern` / `injected_noise` for GP are also computed against the subsample distribution (`reference=y_train_gp`), so GP's quantile and outlier cut-points differ slightly from every other model's.",
   "why_it_matters": "This fires on LogD: n=5039 -> per-fold train 4031, minus the 20% val carve = 3225 > GP_MAX_N=2000. Any per-molecule join across models \u2014 the obvious analysis, 'do QRF and GP flag the same corrupted molecules?' \u2014 keyed on (dataset, fold, split, sample_idx) will silently pair GP row k with a completely different molecule for every other model, producing a spurious near-zero cross-model agreement.",
   "repro": "python3 on tests/data_cache/openadmet_train.csv:\n  LogD: n=5039 -> per-fold train=4031, minus val -> 3225  (GP_MAX_N=2000 exceeded: True)\n  Caco-2 Efflux: n=2161 -> 1383 (not exceeded)\nhERG n=1415 -> ~906 (not exceeded). So GP-on-LogD is the affected cell: 4 reps x 6 strategies = 24 tasks.",
   "fix": "Carry the real row index. Where the GP subsample is taken, keep `gp_idx` and write `'sample_idx': int(gp_idx[i])` for GP's train_oof rows (and `np.arange(len(y_train))` for everyone else), or add an explicit `train_row` column set from that index array. Passing `gp_idx` down into `run_tree_experiment` alongside `y_train_gp` is the smallest change."
  },
  {
   "severity": "MEDIUM",
   "file": "/Users/apunt/repos/NoiseInject/noiseInject/core.py",
   "line": "104-114 (_scale_threshold, high_threshold=1.0 / low_threshold=-1.0)",
   "problem": "`_scale_threshold` uses ABSOLUTE cut-points at +/-1.0 on the raw label, and the diff's comment explicitly keeps them reference-independent. On ChEMBL-hERG the labels are pChEMBL in [4.07, 9.41], so 100% of molecules land in the `y >= 1.0` branch: `noise_scale` and the new `noise_pattern` column are a CONSTANT (2*sigma) for every molecule, train and test.",
   "why_it_matters": "Question (B) of the whole re-run \u2014 'does uncertainty track where the noise is?' \u2014 is mathematically undefined for the threshold strategy on hERG: any Spearman/Pearson of `uncertainty` against a constant `noise_scale`/`noise_pattern` is NaN. That is 7 models x 4 reps = 28 tasks (each 330 model fits) producing an uninterpretable column, and it reproduces exactly the known QM9 threshold degeneracy (min gap 2.08 eV > 1.0 eV). It also means 'threshold' on hERG is just legacy at double dose, so it is not an independent strategy there either.",
   "repro": "python3 -c \"import pandas as pd; y=pd.read_csv('/Users/apunt/repos/KIRBy/tests/data_cache/chembl_herg_ki.csv')['pChEMBL']; print(y.min(), y.max(), (y>=1).mean())\"\n  -> 4.07 9.41 1.0\nAnd on the injector directly (pKi-like y ~ N(7, 1.2)):\n  threshold  te_pattern unique vals=1  tr_pattern unique=1     (legacy also 1, by design; quantile 2, outlier 2, hetero/valprop n)\nLogD (range -2.00..5.20, 84.3% >= 1, 1.1% <= -1) and Caco-2 log10 (range -0.59..2.02, 9.2% >= 1, 0% <= -1) are non-degenerate but heavily one-sided.",
   "fix": "Either pass reference-derived cut-points for threshold (e.g. `high_threshold=np.quantile(reference, 0.9)`) so the strategy is meaningful on non-centred label scales, or drop threshold from the hERG arm of the job matrix and record in the runbook that threshold == legacy at 2x dose there. Do not let 28 tasks run to produce a constant column."
  },
  {
   "severity": "MEDIUM",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "796-799 (_oof_predict except/continue); 1231-1251 (train_oof writer)",
   "problem": "`_oof_predict` catches every per-fold exception, prints one line and `continue`s, leaving that fold's slice as NaN. There is no check that any fold succeeded. The writer's gate is `if extras['oof_unc']:` \u2014 dict non-emptiness, not content \u2014 so if all 5 inner folds fail (OOM on a 2048-dim BNN, gpytorch NotPSD, torchbnn error), the code happily writes the full block of train_oof rows with `uncertainty = NaN` and `y_pred = NaN` for every molecule at every sigma.",
   "why_it_matters": "Verified behaviour: with a model that returns no uncertainty, `oof_unc all-NaN: True` while `dict truthy: True`, and the rows are still emitted. Downstream, merge_results.py:76-90 computes `oof_rows = len(sel[sel['split']=='train_oof'])` and marks the cell `OK` whenever that count is non-zero \u2014 so a cell where every single out-of-fold value is NaN is reported as OK in coverage.csv. The failure is only visible by grepping '[oof] fold failed' out of 84 SLURM logs.",
   "repro": "python3 against the shipped module:\n  run_tree_experiment(..., RandomForestRegressor(...), 'quantile', [0.0,0.5], oof_folds=5)\n  -> 'oof=5 None-unc model -> u[0.0]: None  oof_unc all-NaN: True  dict truthy: True'\nmerge_results.py:85 -> \"'status': ('MISSING' if len(sel)==0 else 'NO_OOF' if len(oof_rows)==0 else ...)\" \u2014 never inspects values.",
   "fix": "In `_oof_predict`, count successful folds and return `(oof_mean, oof_unc, n_ok)`; skip writing (or write a status column) when `n_ok == 0`. In merge_results.py add `oof_finite = sel.loc[sel.split=='train_oof','uncertainty'].notna().sum()` and status `ALL_NAN` when it is 0."
  },
  {
   "severity": "LOW",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "1366-1372 (existing-file merge dedupe keys)",
   "problem": "The re-run merge drops previously-saved rows whose (split, strategy, fold) tuple appears in the new frame. `sigma` is not in the key. The docstring above it says 'jobs are split by strategy, so several write the same filename', which implies shared output directories \u2014 the generated SLURM scripts actually give each task its own `--results-root`, so the case does not fire as written.",
   "why_it_matters": "If anyone follows the docstring's premise and points two jobs with different `--sigmas` at one results dir (or resubmits a partial sigma sweep to top up a file), the second write silently deletes the first job's sigmas instead of merging them.",
   "repro": "python3 replay of the merge block:\n  case1 rerun same strategy: rows 15 tags [2]           # correct\n  case2 different strategy: rows 30 strategies ['legacy','outlier']   # correct\n  case3 different sigma same (split,strategy,fold): rows 3 sigmas kept [0.5]   # 0.0 rows silently dropped\n  case4 csv-roundtrip dedupe: rows 3 tags [2]           # correct",
   "fix": "Add 'sigma' to the key list: `keys = [c for c in ('split','strategy','fold','sigma') if ...]`, and fix the docstring to say each task writes its own results-root."
  }
 ]
}