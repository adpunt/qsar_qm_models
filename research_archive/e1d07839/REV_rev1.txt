{
 "findings": [
  {
   "severity": "CRITICAL",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "876, 929, 1195-1216 (and /Users/apunt/repos/NoiseInject/noiseInject/core.py:104-136)",
   "problem": "Question B's target is a deterministic function of the test molecule's own label, and the model is trained to predict that label. `te_scale = injector.noise_scale(y_test, sigma, reference=y_train)` returns h(y_test): for valprop it is sigma*(1+0.05|y|), for hetero sigma*sqrt(0.1+0.05|y|), for quantile/threshold/outlier a step function of y. The code comment at L1194-1197 asserts 'the model never saw this, so it is a genuine prediction target'. That is false. The model sees y in training and is optimised to reproduce it, so any model whose uncertainty varies systematically with |y| or with distance from the label centre scores on B without having learned anything about noise. Uncertainty-grows-in-the-tails is generic (leverage, sparse support at label extremes, wider conditional spread in QRF/GP), so the confound is guaranteed to be present, not hypothetical. Measured on real data with ZERO noise injected: sigma=0 QRF/PDV/logD gives rho=-0.238 vs the valprop/hetero target (p=1.8e-14), threshold -0.192; caco2 QRF/ECFP4 gives +0.346 (p=1.3e-13). The sign is not even consistent across model/rep, which is the signature of an artefact rather than a mechanism. Worse, a sham 'uncertainty' constructed as h(y_pred) - i.e. a model with no uncertainty head whatsoever, on clean labels - scores +0.824 on hetero/valprop, +0.542 on quantile, +0.534 on threshold, +0.450 on outlier. Every real uncertainty estimate in the file is beaten several-fold by simply predicting y.",
   "why_it_matters": "Every headline B number this 504-task run produces is reproducible on clean data by a model that has learned nothing about noise. A positive result would be published as 'models learn where the data is unreliable' when it measures 'uncertainty correlates with |y|, and the noise scale is a function of |y|'. Days of cluster time for a manufactured finding.",
   "fix": "The target must stop being a function of the quantity the model predicts. Two changes, both needed:\n(1) ADD AN X-DEPENDENT NOISE STRATEGY as the decisive arm. Make sigma_i a function of a molecular feature that is NOT the label - e.g. a Murcko-scaffold cluster id, or heavy-atom count - so a good y-predictor gets no free ride and the only route to scoring is genuinely detecting which regions of chemical space carry noisy labels. Without this arm, question B has no defensible positive form.\n(2) For the existing six strategies, never report raw rho. Report Delta_rho = rho_sigma - rho_0 (same model, rep, fold, molecules), and alongside it the sham ceiling rho_sham = spearman(h(y_pred_sigma), h(y_true)) computed from the y_pred column already written. Any rho_sigma below rho_sham carries no noise information beyond predicting y. Equivalently, use partial Spearman of uncertainty against h(y_true) controlling for h(y_pred). This is an analysis-side change and costs nothing extra on the cluster - but it is only possible if the next finding is also fixed.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qB_kill.py\n\nsigma=0 (CLEAN LABELS, no noise ever injected). QRF/PDV/logD n=1008  corr(y,ypred)=0.834\nstrategy    rho(REAL unc, target)  rho(SHAM unc = h(y_pred))\noutlier                    +0.054                     +0.450\nquantile                   +0.047                     +0.542\nhetero                     -0.238                     +0.824\nthreshold                  -0.192                     +0.534\nvalprop                    -0.238                     +0.824\n\nand qB_confound.py, which repeats it across QRF/PDV, QRF/ECFP4, GP/PDV on logD and QRF/ECFP4 on caco2 (caco2 hetero/valprop rho=+0.346, p=1.3e-13)."
  },
  {
   "severity": "CRITICAL",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "866-869, 923-926, 1214",
   "problem": "The sigma=0 negative control is destroyed on the way to disk. At sigma=0 the code writes `tr_scale = np.zeros(...)`, `te_scale = np.zeros(...)`, so the `noise_scale` column is identically 0 for every molecule. The comment at L865-867 calls this 'what makes sigma=0 a real negative control'. It is the exact opposite: a constant column has zero variance, so spearmanr against it returns nan, and the one correlation that would expose the confound in the previous finding cannot be computed from the output at all. This matters more than it looks, because I verified that `noise_scale(y, sigma)` is EXACTLY proportional to sigma for all six strategies (scale(1.0) == 10*scale(0.1) is True for every one). The rank target is therefore identical at every sigma>0 and the only thing that varies across the sigma sweep is the uncertainty side - which makes sigma=0 the unique and necessary baseline. The pattern h(y) exists perfectly well at sigma=0; the code simply refuses to record it.",
   "why_it_matters": "The run finishes, 504 tasks of compute are spent, and the control that decides whether the result is real is not in the data. Recovering it post hoc requires the per-fold training labels to rebuild quantile/outlier cut-points; those only survive in the train_oof rows, only for models in UNCERTAINTY_MODELS, and only when a fold did not fail - so for any cell that lost its OOF rows the control is unrecoverable and the cell must be rerun.",
   "fix": "Add one column, `noise_scale_pattern`, written at EVERY sigma INCLUDING 0, as `injector.noise_scale(y, 1.0, reference=y_train)` - the sigma-invariant shape. Zero extra compute, no extra fits, no RNG consumption (`noise_scale` is a pure function; verified it does not advance the generator). Keep `noise_scale` as it is for the dose. Then rho_0 against `noise_scale_pattern` is directly computable and Delta_rho = rho_sigma - rho_0 becomes the reported statistic. Do this before submitting, not after."
  },
  {
   "severity": "CRITICAL",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "776-802, 1245-1247",
   "problem": "Question A as framed - out-of-fold uncertainty vs the recorded injected noise - has an answer that is zero by construction for the Gaussian arm, and the run cannot discover otherwise. Under `legacy` every molecule gets an identical noise SCALE (verified: np.unique(scale) == [0.6] at sigma=0.6), so the only per-molecule variation in `injected_noise` is the realised epsilon_i. `_oof_predict` explicitly excludes molecule i's label from the model that scores it (that is the whole point of L780-786). epsilon_i is an i.i.d. draw that entered the data through exactly one row, and that row is held out. It is therefore independent of everything the scoring model saw, so E[corr(uncertainty_i, |epsilon_i|)] = 0 exactly. Confirmed on real molecules (655 hERG SMILES, 393 Murcko scaffolds, ECFP4, RF per-tree spread): rho = +0.0056 with random OOF and -0.0492 with scaffold OOF, both inside the 200-draw permutation null [-0.083,+0.069] and [-0.077,+0.062]. This is a tautology, not a measurement. The adjacent trap is worse: correlating |y_noisy - oof_pred| against |epsilon| gives +0.153 here and rises with model accuracy, because the residual CONTAINS epsilon by construction. That quantity measures model fit, not noise detection, and would read as a strong positive result.",
   "why_it_matters": "If the Gaussian A-result is reported as a finding ('models fail to flag corrupted labels'), it is circular - the design forbids any other answer. If anyone reaches for the residual version to get a non-null number, they get a large positive correlation that is pure arithmetic. Either way the cluster time buys nothing.",
   "fix": "State the Gaussian A-arm as what it is: a sanity check on the cross-fitting, where any |rho| falling OUTSIDE the permutation null indicates leakage in `_oof_predict` and nothing else. Ship the permutation null (shuffle `injected_noise` within (fold, sigma), 200+ draws) alongside every A number so the null is visible. The only informative A analysis is against `noise_scale`, not `injected_noise`, and only for the heteroscedastic strategies where the scale actually varies - but that immediately inherits the |y| confound of the first finding and needs the same Delta_rho / partial-correlation controls. Never correlate any residual-derived quantity against injected_noise.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qA.py\n\nRANDOM OOF (as shipped)\n  QUESTION A: spearman(uncertainty, |epsilon|) = +0.0056  permutation null 95% CI [-0.0825,+0.0685] -> INSIDE NULL\n  TRAP:       spearman(|y_noisy - oof_pred|, |epsilon|) = +0.1534  <- trivially large, contains eps by construction\nSCAFFOLD OOF\n  QUESTION A: spearman(uncertainty, |epsilon|) = -0.0492  permutation null 95% CI [-0.0768,+0.0621] -> INSIDE NULL"
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/qsar_qm_models/slurm_scripts_uncertainty_rerun/generate_scripts.py",
   "line": "16-19",
   "problem": "The design doc names the Gaussian arm as question B's control: \"Gaussian ('legacy') hits every molecule equally and is therefore the control.\" It cannot serve that role. Under legacy, `noise_scale` is constant across molecules at every sigma, so Spearman against it returns nan, not zero. A control has to produce a number near a known null; this one produces no number at all. Verified on real logD labels: legacy target uniq = [1.0], std = 0, rho undefined for all four (dataset, model, rep) cells I tested. Separately, even if it returned 0 it would not control the actual confound, which is that the OTHER five targets are functions of |y| while legacy's is not - so legacy differs from them in the confounded variable itself, not just in the presence of a learnable pattern.",
   "why_it_matters": "The run's stated control is vacuous, so a positive B result on valprop/hetero has nothing to be compared against, and the natural fallback ('legacy shows nothing, the others show something') is an artefact of one target being constant and the others being monotone in |y|.",
   "fix": "Drop legacy as the B control. The control is sigma=0 within the SAME strategy (needs the `noise_scale_pattern` column above), plus the sham ceiling h(y_pred). Update the docstring in generate_scripts.py and RUNBOOK.md so nobody analyses to the wrong plan. Keep legacy in the run for R2/robustness and as the question-A leakage check.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qB_confound.py\n  legacy  target CONSTANT (std=0) -> rho undefined (nan). uniq=[1.]   (all 4 dataset/model/rep cells)"
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/NoiseInject/noiseInject/core.py",
   "line": "126-135",
   "problem": "`hetero` and `valprop` are the SAME arm for any rank-based question-B or question-A analysis. hetero returns sigma*sqrt(0.1+0.05|y|) and valprop returns sigma*(1+0.05|y|); both are strictly increasing in |y|, so spearman(hetero_scale, valprop_scale) = 1.000000 exactly, and spearman(|y|, each) = 1.000000. On real logD data the two produce byte-identical correlations at every sigma (see the qB_drift table: -0.238/-0.238, -0.237/-0.237, ... -0.110/-0.110 for all 11 sigma levels). GP/PDV likewise +0.127/+0.127, caco2 +0.346/+0.346.",
   "why_it_matters": "Two of the six strategies are one strategy. 168 of the 504 tasks (2 of 6 strategies x 84) produce duplicate rank information for the uncertainty questions, and any 'hetero and valprop both show the effect, so it replicates' statement is a single observation reported twice. Roughly a third of the run's uncertainty compute is redundant.",
   "fix": "Keep both for the R2/robustness sweep where the dose magnitude differs (RMS dose is not equal), but for the uncertainty analysis treat them as one arm and say so - never cite them as independent replication. If cluster time is tight, drop `hetero` from the --strategies list in the uncertainty scripts and reclaim 84 tasks; better, spend those tasks on the X-dependent strategy from the first finding.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qB_mech.py\n  spearman(hetero_scale, valprop_scale) = 1.000000\n  spearman(|y|,hetero)=1.000000  spearman(|y|,valprop)=1.000000"
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/NoiseInject/noiseInject/core.py",
   "line": "104-114",
   "problem": "`_scale_threshold` uses ABSOLUTE cut-points high_threshold=+1.0 / low_threshold=-1.0 that are never overridden (the pipeline passes no strategy_params). hERG labels are pChEMBL/pKi values, which lie roughly in [4,11] - every single molecule is >= 1.0, so the noise scale is constant 2*sigma across the whole dataset. The question-B target has zero variance and rho is nan; the question-A `noise_scale` column is likewise constant. Verified: simulated pKi in [4,11] gives np.unique(scale) == [2.] and std == 0. This is the same defect already recorded for QM9 in the project memory (min gap 2.08 eV, 100% of molecules get 2 sigma), reappearing here on a second dataset. On logD the arm is not void but is badly degenerate: 860 of 1008 molecules in the high class and 148 in the low, with the low class being the only variation - and on caco2 the split is similarly lopsided.",
   "why_it_matters": "All 84 threshold tasks on herg_ki (12 tasks x 7 models) produce a constant target and cannot answer question B at all. That is a sixth of the hERG uncertainty run burned for a nan. On logD/caco2 the arm is a near-constant binary that will yield unstable, sign-unstable correlations that look like signal.",
   "fix": "Before submitting, either (a) pass --strategy-params to set threshold cut-points per dataset to real quantiles of that dataset's labels, or (b) drop `threshold` from the --strategies list for herg_ki. Whichever is chosen, add a preflight assertion in preflight.sh that np.std(noise_scale(y_dataset, 1.0)) > 0 for every (dataset, strategy) pair the run intends to submit - it is a two-line check that would have caught this and the legacy case.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qB_mech.py\n  4) hERG-like labels (pKi ~4-11): unique scales = [2.]  -> std=0.0  => target CONSTANT, rho undefined\n  3) logD class balance: threshold {0.1: 148, 2.0: 860}  n=1008"
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "791-792",
   "problem": "`_oof_predict` splits the training set with `np.random.RandomState(seed).permutation(n)` - a RANDOM split. Every other split in this pipeline is scaffold-grouped (GroupKFold on Murcko scaffolds, L1117-1120), and the project's standing requirement is that all experiments use scaffold splits. `_oof_predict` is never passed `groups` and `run_tree_experiment`/`run_neural_experiment` never receive them, so it cannot be made scaffold-aware without a signature change. Consequence: the OOF fit set contains close analogues of every held-out molecule, so OOF uncertainties are systematically smaller and OOF accuracy systematically better than the scaffold-split test uncertainties written into the same CSV under the same column name. Measured on 655 real hERG molecules / 393 scaffolds: random OOF gives RMSE 0.886 and mean uncertainty 0.888; scaffold OOF on the identical data gives RMSE 0.964 and mean uncertainty 0.972 - about 9% wider.",
   "why_it_matters": "`split='train_oof'` and `split='test'` rows land in one file with one `uncertainty` column on two different scales. Any analysis that pools them, or that compares an A-result against a B-result, is comparing an interpolation regime against an extrapolation regime. It also silently breaks the project rule that every reported number comes from a scaffold split, which is a reviewer-facing problem for the paper.",
   "fix": "Thread the scaffold `groups` array (already computed at L1037) down through run_dataset -> run_tree_experiment / run_neural_experiment -> `_oof_predict`, subset it by train_idx then train_idx_local, and replace the permutation with sklearn GroupKFold(n_folds).split(X, y_noisy, groups_train). This is a ~10-line change and must go in before submission, because reruns cost the full 6x OOF budget.",
   "repro": "python /private/tmp/claude-501/-Users-apunt-repos-qsar-qm-models/e1d07839-2376-4458-891b-01d0a9e0433b/scratchpad/qA.py\n  RANDOM OOF (as shipped):        OOF RMSE vs CLEAN y = 0.886   mean uncertainty = 0.888\n  SCAFFOLD OOF (paper standard):  OOF RMSE vs CLEAN y = 0.964   mean uncertainty = 0.972"
  },
  {
   "severity": "HIGH",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "929-933, 945",
   "problem": "The four neural uncertainty models (BNN-Full, VBLL-Full, MLP-BNN-Full, MLP-VBLL-Full) early-stop against a validation set whose labels are NEVER noised. `run_neural_experiment` injects into y_train only and passes the untouched `y_val` straight through (L932, and identically in the OOF closure at L945); `train_neural_regression` selects the checkpoint minimising loss on that clean set (L727-737). At every sigma, and for every strategy, these models are explicitly selected NOT to fit the injected noise. The three non-neural uncertainty models (QRF, NGBoost, GP) get no such protection - they fit the corrupted labels outright.",
   "why_it_matters": "The 30-forward-pass predictive spread of the Bayesian nets is calibrated against noise-free labels, which suppresses exactly the quantity questions A and B set out to measure, and does so for 4 of the 7 models only. Any 'Bayesian nets track the injected noise less well than QRF' conclusion is confounded by the early-stopping signal, not by the uncertainty formalism. That comparison is the point of running seven models.",
   "fix": "Inject the same strategy and sigma into y_val using the same injector before passing it in, so validation shares the training corruption (this is also the realistic setting - in practice you do not hold out clean labels). If the clean-val behaviour is deliberate, it must be documented in RUNBOOK.md and no tree-vs-neural uncertainty comparison may be drawn from this run. Note the val set is carved from train at L1132-1136 and is disjoint from X_train, so the OOF folds do not leak into it - that part is sound; the issue is purely that its labels are clean."
  },
  {
   "severity": "MEDIUM",
   "file": "/Users/apunt/repos/qsar_qm_models/slurm_scripts_uncertainty_rerun/merge_results.py",
   "line": "29-48, 66-71",
   "problem": "`_read_all` accumulates every per-task uncertainty CSV into a Python list and then calls a single `pd.concat`. Row arithmetic: logD has ~1008 test molecules per fold, implying N~5040 and n_train~3225 after the 20% val carve, so one task writes 11 sigmas x 5 folds x (1008 + 3225) = 232,815 rows. With 3 datasets x 4 reps x 6 strategies x 7 models = 504 tasks that is ~117 million rows for `uncertainty.csv`. Measured on a representative frame with the same 18 columns and the same object dtypes this code produces, that is 776 bytes/row resident = ~91 GB for the concatenated frame, and pd.concat peaks at roughly 2x = ~182 GB. The output CSV alone is ~10.6 GB.",
   "why_it_matters": "The merge is the last step, after all 504 tasks have finished. It will OOM on any normal login or compute node, and the failure arrives only once the entire multi-day run is complete. The coverage.csv report - explicitly documented as 'the thing to read before trusting anything' - is computed from that same in-memory frame and dies with it, so there is no way to tell what ran and what is missing.",
   "fix": "Do not build one frame. Write uncertainty rows to Parquet partitioned by (dataset, model, rep, strategy), or append per-task CSVs to a single file with a streaming writer and never read it back whole. Build coverage.csv incrementally: for each task file read only the small set of key columns (usecols=['split','strategy','fold','sigma']) and accumulate counts, so coverage survives independently of the bulk data. Also downcast: `split`, `strategy`, `dataset`, `model`, `rep` and the four `_task_*` columns are low-cardinality and should be pandas category dtype, `_source` should be dropped or replaced by a small integer id - together that removes most of the 776 bytes/row.",
   "repro": "python - <<'PY'  (full script in the transcript)\n  bytes per row in RAM (object dtypes, as _read_all builds it) = 776\n  117,338,760 rows -> 91 GB resident, and pd.concat peaks at ~2x = 182 GB"
  },
  {
   "severity": "MEDIUM",
   "file": "/Users/apunt/repos/KIRBy/tests/alternative_data_noise_robustness.py",
   "line": "793-802",
   "problem": "`_oof_predict` catches any fold exception, prints, and `continue`s, leaving `oof_mean`/`oof_unc` as NaN for that fold's molecules. Those all-NaN rows are still written to the CSV (the writer at L1232-1249 has no NaN guard - it only checks `if ou is None`, which never fires once the key is set). The molecules that go missing are one whole fold of a random permutation, so the survivors are a non-random subset with respect to nothing in particular, but the row COUNT silently drops by 20% per failed fold while coverage.csv reports the cell as 'OK' (it checks `sel['fold'].nunique() < 5` on the OUTER scaffold fold, not the inner OOF fold, so an inner failure is invisible).",
   "why_it_matters": "A GP OOM or an NGBoost failure on one inner fold silently removes a fifth of the question-A data for that cell, and the coverage report designed to catch exactly this will mark it OK. Any per-cell correlation is then computed on a partial, undocumented subset - and with 504 cells a handful of quiet failures is near-certain.",
   "fix": "Record the OOF failure count in `extras` and propagate it into the written rows (e.g. an `oof_folds_ok` column), or skip writing train_oof rows entirely for a sigma where any fold failed. Add `n_oof_nan` to coverage.csv and flag any cell where OOF uncertainty is NaN for more than 0% of rows. At minimum, make `_oof_predict` re-raise after the second failed fold rather than returning a quietly-truncated array."
  }
 ]
}