# Superseded, kept not deleted

Nothing here is the answer. The answer is `../CHOSEN_SETTINGS.md` and `.json`,
and the rules behind it are `RERUN_PLAN.md` section 5.7RULES.

| file | why it is here |
|---|---|
| `FINAL_LIST.md` / `.json` | chose a setting from the search alone, tiebroken on the ONE representation that drew it. The rule is the mean of the changes across ALL representations, which the search cannot supply. RERUN_PLAN.md 5.7an. |
| `DECISIONS.md` | the three filters applied per model-and-representation pairing. Superseded by one setting per model per dataset. |
| `per_model_c_*.csv`, `per_model_x_*.csv` here | runs made before 2026-09-01, when the candidate pool was built only from the representations being scored, so it held three settings instead of seven. RERUN_PLAN.md 5.7RULES. |
| `best_by_pairing.before_merge_*.json` | a backup taken before a merge on 2026-08-31. |
| `recorded_for_*.json` | written by the superseded list above. |
