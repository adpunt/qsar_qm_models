{
  "what_this_is": [
    "Failed tasks whose cause has since been fixed, so a resubmission is not a repeat",
    "of the same crash. failed_tasks.py prints a resubmission line for a FAILED task",
    "ONLY when its cause appears here AND the commit named below is an ancestor of the",
    "HEAD of the checkout the tool runs in. On the cluster that checkout is what the",
    "jobs run, so the claim 'the cause is fixed' is checked against the code rather",
    "than asserted in a document. A commit that is not there prints a refusal, not a",
    "resubmission line.",
    "",
    "Fields. `commit` is the fix. `proof` is the command that demonstrates it and needs",
    "no cluster. `error_matches` are substrings of the log's last error; a task whose",
    "log IS readable is matched on those alone, so a task that died on something else",
    "is never swept in by its position in the array. `task_rule` is used ONLY when the",
    "log cannot be read, which is the case on a laptop and where a rebuilt script",
    "changed the --output name. `error_source` is the file those strings are raised",
    "from, and the test checks each one is still in it, so a reworded error message",
    "cannot silently stop matching."
  ],
  "fixed_causes": [
    {
      "id": "sns-all-zero-vector",
      "what": "Methane, ammonia and water each carry one Morgan substructure, and it occurs in exactly one molecule, so none of them can reach a top-1024 chosen by how often a substructure appears in training. Their Sort & Slice vector is all zeros and the featuriser guard refuses to train on a molecule with no features.",
      "fix": "Those three are dropped from every representation, found by the property rather than by name, after the shuffle and the split, so nothing already on disk changes.",
      "commit": "62f1fe2",
      "proof": "python scripts/test_sns_zero_exclusion.py",
      "error_source": "scripts/process_and_train.py",
      "error_matches": [
        "Sort & Slice produced an all-zero count vector",
        "A molecule with no enumerable substructures"
      ],
      "job_name_matches": "^qm9[0-9]_",
      "task_rule": {
        "kind": "qm9_representation",
        "representation": "sns",
        "why": "The generated script picks the representation with REPS[i % n_rep] (slurm_scripts_qm9_rerun/generate_scripts.py, the `rep=` line), so the tasks this cause touches are every index whose remainder is Sort & Slice's position in REPS. The position is read off the generated script where it is on disk; the two numbers below are used only when it is not, and they are the six representations in their generated order with Sort & Slice last.",
        "assumed_modulus": 6,
        "assumed_residues": [
          5
        ]
      }
    },
    {
      "id": "gp-cap-noise-length-mismatch",
      "what": "An exact Gaussian process is cubic in its training count, so cap_gp_training_set subsamples to GP_DEFAULTS['max_train_n'] -- 5,000 of QM9's 8,000 training molecules, on every representation and at every noise level. It returned only the COUNT and threw the selection away, so the caller handed the out-of-fold pass 5,000 rows while the noise record still described all 8,000, and the guard refused rather than pairing them by position. The whole (noise level, replicate) cell was then lost, accuracy rows included, and the task exited non-zero.",
      "fix": "The cap returns the indices it kept and both Gaussian-process fitters pass them to the out-of-fold scorer as train_slice, so the rows scored and the noise recorded for them are the same molecules in the same order.",
      "commit": "c223ec3",
      "proof": "python scripts/test_gp_cap_matches_noise.py",
      "error_source": "models/models.py",
      "error_matches": [
        "out-of-fold scoring for"
      ],
      "job_name_matches": "^qm9[0-9]_(gauche|gauche_rbf|heteroscedastic_gp)$",
      "no_task_rule_why": "There is deliberately no position fallback. The cause touches whichever representations run the out-of-fold pass, which is three of the main grid's six and all three of the deep run's -- more than one remainder, which the qm9_representation rule cannot express. The QM9 .out files are on disk and readable, which is the case this entry is for; a Gaussian-process task whose log cannot be read gets no resubmission line, and that is the safe answer rather than a guess at its index.",
      "not_the_laboratory_pipeline": "KIRBy's runner does not call cap_gp_training_set at all -- it loads models/model_defaults.py only for the shared parameter spec -- so its GP fits the whole training set and never had this failure. val_gp, val_gp-hetero and val_gp-tanimoto are therefore NOT matched here."
    }
  ]
}
