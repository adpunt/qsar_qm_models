/rust_processor/target/
*.dylib
*.pth
*.pt
*.pkl
*.mmap
__pycache__/
*.pyc

data/smiles_cache/
slurm_scripts_anova/
slurm_scripts_uncertainty/
scripts/test_runtime_scaling.sh
paper.md
paper.tex

# Research archive: keep the reviews, code and extracted text in git; not the source PDFs
research_archive/**/*.pdf
research_archive/**/*.html
research_archive/**/*.xml

# Cross-check working files: the label and group columns are regenerated from
# data/QM9/ by scripts/crosscheck_injectors.py, so they are derived, not source.
scratchpad_crosscheck/
rust/reference/target/

# Derived feature caches for the parity tests - regenerate with the scripts
results/parity_tests/_cache/

# --- added 2026-08-27 ---------------------------------------------------
# Build output and downloaded data, none of it source. rust/target alone is
# over a gigabyte and contains files GitHub would reject outright, and it was
# not covered by the /rust_processor/target/ line above.
rust/target/
.DS_Store
_build_paper/
_build_addfiles/
scripts/vina_1.2.6_mac_x86_64
scripts/vina_1.2.6_mac_x86_64.zip

# QM9 as downloaded by torch_geometric: 589 MB, regenerated by
# process_and_train.py on first run. gdb9.sdf and data_v3.pt are each far over
# GitHub's file limit.
data/QM9/

# A probe output kept in the archive by accident; over 20 MB and derived.
research_archive/e1d07839/probe.csv
